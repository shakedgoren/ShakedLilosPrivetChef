# שקד לילוז · שאלון הזמנות — מסמך המשכיות

> עודכן: 1 בספטמבר 2026. הקובץ מתאר את המצב **הנוכחי** של הפרויקט.
> אם קראת אותו — יש לך את כל מה שצריך כדי להמשיך בלי שאלות פתיחה.

---

## הפרויקט

קובץ HTML **יחיד** — `chef-questionnaire.html` (~3,775 שורות, ~282KB).
שאלון הזמנות אינטראקטיבי לעסק קייטרינג/שף פרטי.
Vanilla HTML/CSS/JS. בלי frameworks ובלי dependencies חיצוניים מלבד גופנים
מ-Google Fonts שנטענים ב-`<link>`.
מבנה: סקריפט קטן ב-`<head>` (מצב תצוגה), `<style>` אחד גדול, ו-`<script>` אחד
גדול בתחתית. **RTL מלא.**

### Git · בנייה · פרסום

הפרויקט תחת git ומחובר ל-GitHub:
**`https://github.com/shakedgoren/ShakedLilosPrivetChef.git`** (ענף `main`).

**מקור אמת יחיד:** `chef-questionnaire.html`. `build.sh` מייצר ממנו את `site/`:

```
site/index.html   ← עותק של chef-questionnaire.html
site/_headers     ← מ-static/_headers  (noindex + כותרות אבטחה)
site/robots.txt   ← מ-static/robots.txt
```

`site/` **מוחרגת מגיט** ונבנית מחדש בכל פרסום — כך אי אפשר לפרסם בטעות
גרסה ישנה. `netlify.toml` מריץ `bash build.sh` ומפרסם את `site`.

**אתר נטליפיי:** `shaked-liloz-orders` (siteId `a93fd286-c9fe-4b7d-9d76-630fa23da80b`)
→ https://shaked-liloz-orders.netlify.app

**⚠ החיבור בין הריפו לנטליפיי טרם בוצע.** כרגע הפרסום ידני. להשלמה:
נטליפיי ← Site configuration ← Build & deploy ← Link repository.

**מה שלא נכנס לגיט** (`.gitignore`): `site/`, הגיבויים הידניים
(`chef-questionnaire.backup*.html`), תיקיית ״העתקים ושחזורים - לא לגעת!״,
וקבצי zip.

---

## מבנה הניווט · ארבע רמות

```
מסך ראשי (5 כרטיסים)  →  [מסך משנה]  →  הזרימה  →  סיכום
        ↓
   עמוד האודות
```

**ארבעה מחמשת הכרטיסים נכנסים ישר לזרימה.** רק ״חוויה בלתי נשכחת״ פותח מסך
משנה. כרטיס ב-`HUBS` עם שדה `flow` מוביל לזרימה; כרטיס עם `key` פותח מסך משנה.

המשתנה `view` מחזיק את המסך הנוכחי: `'hub'` · `'sub'` · `'about'` · `'flow'`.
`hubKey` מחזיק איזה מרכז פתוח. `idx` הוא השלב בתוך הזרימה (`-1` = לא בזרימה).

**חשוב:** המשתנה נקרא `view` ולא `screen` — `screen` מתנגש עם `window.screen`.

### חמשת הכרטיסים

| # | כרטיס | לאן |
|---|---|---|
| 1 | שלישי של קוסקוס 🏡 | ישר ל-`couscous` |
| 2 | שישי של מטעמים | ישר ל-`friday` |
| 3 | מארזי ספיישל 🥂 | ישר ל-`holiday` |
| 4 | מגשי פירות 🍓 | ישר ל-`fruit` |
| 5 | חוויה בלתי נשכחת 👨🏻‍🍳 | מסך משנה: שף · טאבון |

**מגשי אירוח הוסרו לגמרי** (23.8.26). ״מארזי חג״ נקרא עכשיו ״מארזי ספיישל״
ו״מסודרים לשישי״ נקרא ״שישי של מטעמים״ — מפתחות הזרימה נשארו `holiday` ו-`friday`.

הנתונים ב-`HUBS` ו-`SUB_ITEMS`. **מסך המשנה הוא אותו רכיב קרוסלה של המסך
הראשי** — `renderCover()` מזין אותו ברשימת פריטים אחרת. אין שכפול.

### שבע הזרימות

| flow | שלבים | תיאור |
|---|---|---|
| `chef` | c1–c6 + last | ארוחת שף |
| `taboon` | t1–t7 + last | עמדת טאבון |
| `holiday` | h1–h3 | מארזי חג |
| `friday` | h1–h3 | מסודרים לשישי |
| `fruit` | h1–h3 | מגשי פירות |
| `couscous` | **h2–h3** | שלישי של קוסקוס — אין עמוד בחירת מארז |

חמש הזרימות עם h1 חולקות שלבים; ההפרדה דרך שדה `cat` ב-`BOXES`.
`last` משותף ל-chef ול-taboon.

**להוספת קטגוריית מארזים חדשה** — שורה ב-`FLOWS`, שורה ב-`BOX_FLOWS`,
רשומה ב-`SUB_ITEMS` תחת המרכז הנכון, ורשומות ב-`ORDER_LABEL` / `CAT_COPY` / `BOXES`.
**אין צורך לגעת בשום תנאי לוגי אחר** — `isBoxFlow()` מרכז את כולם.

---

## שני מסכים שנבנו מחדש · חשוב להכיר

### שישניצל (`friSchnitzel`, בתוך `friday`)

שני מסלולים, נבחרים ב-`H.schMode`:

- **אישי · 50 ₪ לחלה** (`SCH_PERSONAL`). סטפר כמות (`H.schCount`), ולכל חלה
  מילוי משלה ב-`H.schFill[i] = {tops:[], eggplant:'', matbucha:''}`.
- **מארז משפחתי · 160 ₪** (`SCH_FAMILY`). `H.schForm` = 4 חלות אישיות או
  חלה משפחתית, והתוספות נבחרות פעם אחת ב-`H.schBox`.

**נבנה מחדש 23.8.26 לפי מפרט של שקד.** שני מצבים: `לפי יחידה` ו-`מארז`.

**העיקרון המרכזי: סוג השניצל קובע את רשימת התוספות.** `SCH_TYPES` מחזיק
שני סוגים, ולכל אחד `tops` משלו:

| הסוג | מחיר יח׳ | מחיר מארז | התוספות |
|---|---|---|---|
| שניצל דק בציפוי פירורי לחם | 50 ₪ | 200 ₪ | מטבוחה · טחינה · חציל בטמפורה · כרוב סגול · פלפל חריף |
| פילה בציפוי טמפורה | 60 ₪ | 250 ₪ | איולי עמבה · איולי עשבי תיבול · עלי רוקט · מלפפון חמוץ |

**התמחור נגזר מהסוג, ולא ממחיר אחיד.** במסלול ״לפי יחידה״ כל חלה יכולה
להיות סוג אחר, ולכן `schUnitsTotal()` מסכם את החלות אחת־אחת. **חלה שטרם
נבחר לה סוג לא מתומחרת** — המחיר גדל תוך כדי הבחירה.
המחיר מוצג על צ׳יפ הסוג עצמו (`.add`), כדי שההפרש ייראה ברגע הבחירה.
`SCH_MIN_UNIT` נגזר אוטומטית ומזין את ״החל מ 50 ₪״ בכרטיס.
`SCH_PERSONAL` ו-`SCH_FAMILY` **הוסרו** — אין יותר מחיר אחיד.

**המארז הוא 5 חלות** (`SCHNITZEL_FORMS`), לא 4. שתי הצורות — 5 חלות
אישיות או חלה משפחתית — באותו מחיר.

**החלפת סוג מאפסת את התוספות** (`fill.tops = []`) כי הרשימות לא חופפות.
`schTops()` לא מחזיר כלום כל עוד אין סוג.

`fill` = `{type, tops[]}` — ב-`H.schFill[i]` לכל חלה, וב-`H.schBox` למארז.

**קוקוט רוטב** — `H.schCocotte` = `{שם רוטב: כמות}`, `COCOTTE_PRICE` = 3 ₪.
כמות חופשית להזמנה, נכנס ל-`boxPrice()`.

**אין בחירת ציפוי לחלה** — כולן מגיעות עם שומשום.
**הכנפיים הן מתנה** ברוטב מפתיע שמתחלף כל שבוע — הערה בלבד (`SCH_WINGS_NOTE`),
לא בחירה. היה רגע שבו תוכנן כבחירה, ושקד תיקנה. לשתיים מהן יש תת־בחירה ב-`SCH_SUB`
(מטבוחה → מתוקה/חריפה, חציל מצופה → טמפורה/קמח). **סימון תוספת כזו פותח
חלונית** (`#sch_pop`); הסוג מוצג על הצ׳יפ (״מטבוחה · חריפה״); לחיצה נוספת
פותחת לשינוי; וההסרה נעשית מתוך החלונית.

מחיר הכרטיס בעמוד הבחירה מוצג כ״החל מ 50 ₪״ (`b.schnitzel` ב-`renderBoxCards`).

### הפסטות (t4 בטאבון)

`S.pastas = [{shape, sauce, up}, …]` — שתיים כלולות (`T_BASE_PASTAS`).
בעמוד רואים **רק את הצורות**; לחיצה על צורה פותחת את `#pasta_pop` עם
הרטבים (מה-`<template id="sauce_tpl">`) וחמשת שדרוגי המנה
(מה-`<template id="pastaup_tpl">`). **השדרוג נבחר בנוסף לרוטב, לא במקומו.**

כפתור `#pasta_add` מוסיף פסטה; לכל פסטה מעבר לשתיים יש ✕ להסרה.

---

## שפת העיצוב · Liquid Glass · חצות ונחושת

כל הצבעים והחומרים הם משתני CSS. שינוי פלטה = עריכת המשתנים בלבד.

- **שני מצבי תצוגה.** `:root` הוא הכהה; `:root[data-theme="light"]` הוא הבהיר.
  **בבהיר סולם הנחושת מתהפך:** `--brass-lit` הוא הגוון הכהה, כי הוא זה שצריך
  להיקרא על רקע בהיר. `--oyster` בבהיר הוא `#553A26` — חום כהה מכוון,
  לא כמעט־שחור. יחס ניגודיות 9.86:1.
- **מסך פתיחה.** לוגו ← פתיחת הסיפור ← ״מה מרימים היום?״ ← קרוסלת `scroll-snap`
  עם 3 כרטיסים ← מגש 3 מכסים קטנים שמשמש גם כתצוגה וגם כניווט.
- **פתיחת הסיפור.** `.teaser` — שורת פתיחה גדולה ב-Suez One עם ברק נחושת
  שעובר עליה פעם אחת, משפט תומך, קו נחושת דק, וכפתור ״לסיפור המלא״.
- **כפתור ״לסיפור המלא״.** בלי מסגרת. קו נחושת נמשך, ומכסה קטן מתרומם ומעלה
  אדים. יש גם מצב `:active` כי **בנייד אין ריחוף**.
- **שלבים פנימיים.** כל שלב הוא לוח זכוכית (`.step.sheet`) עם מכסה שמתרומם
  בכניסה. מוזרקים ב-JS ב-`dressSteps()`.
- **סרגל עליון:** מוסתר **רק במסך הראשי** (`view!=='hub'` מפעיל `has-chrome`).
  **מתג התצוגה יושב מחוץ לסרגל** (`.themex`, `position:fixed`).
- **אין סמן עכבר מותאם.** היה כזה (סכין ומזלג) והוסר לבקשת הלקוחה.
- **אייקוני הפסטה** — ערכה ״תלת־רבע״: כל צורה בזווית עם הפתח או החלל נראים.
  קווי הפנים נושאים `class="in"` ומצוירים דק יותר. צבועים בנחושת.

### מעברים בין המסכים

| מעבר | אפקט |
|---|---|
| מהמסך הראשי → מסך משנה | `x-lid` — מכסה יורד, סוגר, מתרומם |
| כל שאר המעברים | `x-out` / `x-in` — הקלפים עפים ומתחלפים |

מנוהל ב-`coverSwap(kind, fn)`. כשיוצאים ממסך בלי כרטיסים (שלב בזרימה או
האודות) מדלגים על שלב היציאה, אחרת נוצרת השהיה שבה כלום לא קורה.

---

## החלטות ארכיטקטורה שחשוב לא לשבור

1. **הטקסטים חיים במקום אחד.** רשימת הרטבים יושבת ב-`<template id="sauce_tpl">`
   ומשוכפלת ב-JS לחלונית. אל תשכפל אותה ב-HTML.

2. **`<br>` חייב לעבוד בכל טקסט.** כל הזרקת תוכן משתמשת ב-`innerHTML` ולא
   ב-`textContent`. בהודעת הוואטסאפ `plain()` הופך `<br>` לירידת שורה.

3. **`backdrop-filter` לא מטשטש מתחת ל-`position:sticky`.** לכן הכותרת הדביקה
   ב-h1 **וכל החלוניות המוקפצות** חייבות רקע אטום (`#101B29` / `#FAFBFD`).

4. **`.step.sheet` יוצר הקשר ערמה** וכולא כל `z-index` פנימי. לכן כל חלונית
   מועברת ב-JS אל `.wrap` בזמן הבנייה — לוח השנה, `#pasta_pop`, `#sch_pop`.

5. **`document` סוגר כל חלונית בלחיצה** (`document.addEventListener('click', closeAllPicks)`).
   כל לחיצה שאמורה **לפתוח** חלונית חייבת `e.stopPropagation()`, אחרת
   החלונית נפתחת ונסגרת באותו רגע. קרה גם בפסטה וגם בשישניצל.

6. **אסור לגעת ב-`transform` של `.slab`.** זהו אזור ה-`scroll-snap`, ושינוי
   גיאומטריה שלו — אפילו באמצע אנימציה — מקפיץ את הקרוסלה לכרטיס השכן.
   כל אנימציית מעבר רצה על `.slab-in` הפנימי. **הקדר האחרון של כל אנימציה
   חייב להתאים בדיוק ל-CSS הרגיל** (`scale(.91)` לצדדיים, `none` לפעיל).

7. **בית הקרוסלה הוא `scrollLeft:0`** — הריפוד הצדדי של `.crail` ממרכז שם את
   הכרטיס הראשון. `coverHome()` מכבה `scroll-snap` לרגע, מאפס, ומחזיר.
   `settleCover()` מתקן חמש פעמים בשנייה הראשונה **ומפסיק ברגע שהמשתמשת נוגעת**.

8. **ב-RTL הסימן של `scrollLeft` שונה בין דפדפנים.** כל מרכוז מחושב כהפרש
   מרכזים דרך `getBoundingClientRect` + `scrollBy`.

9. **`button{color:inherit}` גלובלי — אל תסיר.**

10. **`wrapDigits` עוטף מספרים ב-`.num` ו-₪ ב-`.cur`.** יש `MutationObserver`
    שמריץ את זה מחדש — היזהר מלולאות. הוא צופה ב-`childList` ו-`characterData`
    בלבד, ולכן שינויי `style`/`class` לא מפעילים אותו.

11. **`paintSaladQty` מזהה את רשת הסלטים דרך `[data-cap]`** — רשת מנות הקוסקוס
    חולקת את אותו עיצוב (`.sq-grid`) ובלי המבדיל הזה הן מתנגשות.

12. **`background-clip:text` עטוף ב-`@supports`.** בלי תמיכה שורת הפתיחה
    הייתה נעלמת לגמרי במקום להיראות רגילה.

13. **מצב התצוגה נקבע בסקריפט שב-`<head>`**, לפני הציור הראשון.
    סדר: `localStorage` ← `prefers-color-scheme` ← כהה.

### ארבעה מוקשי CSS שנשרפנו בהם

- **`.pickpop.open{display:block}` מוגדר מאוחר בקובץ** וגובר על כל כלל עם
  סגוליות זהה. חלונית שצריכה `display:flex` חייבת סלקטור חזק יותר —
  `.pickpop.pasta-pop.open`.
- **אחוזי רוחב וגובה בתוך grid שמתכווץ לתוכן מתפרשים זה מול זה** והאלמנט
  יוצא לא ריבועי. הפתרון: `grid-template:1fr / 1fr` נותן מסלול בגודל מוגדר.
- **ריפוד באחוזים על פריט flex מתפרש מול רוחב השורה**, לא מול האלמנט —
  צ׳יפ של 64px קפץ ל-102px.
- **ל-`.chip` הבסיסי יש `min-height:58px`** שגובר על `aspect-ratio`.
  צ׳יפ שאמור להיות ריבועי חייב `min-height:0`.

---

## איפה עורכים תוכן

**המדריך `עריכת-טקסטים.html` מיושן ולא עודכן.** בפועל הכל בתחתית ה-`<script>`:

| קבוע | מה |
|---|---|
| `ABOUT_TITLE` / `ABOUT_TEASER_LEAD` / `ABOUT_TEASER_SUB` / `ABOUT_BODY` | עמוד האודות והפתיח |
| `HUBS` | 3 הכרטיסים הראשיים — `eb`, `name`, `desc` |
| `SUB_ITEMS` | הקטגוריות בכל מרכז |
| `CAT_COPY` | כותרות עמוד המארזים |
| `BOXES` | כל המארזים — `name`, `desc`, `price`, `sub`, `group` |
| `SCHNITZEL_TOPS` / `SCH_SUB` / `SCH_PERSONAL` / `SCH_FAMILY` | השישניצל |
| `MEALS` / `MEAL_ADDONS` / `COUS_MIN_DELIVERY` | הקוסקוס |
| `PASTA_SHAPES` | 7 צורות פסטה + ה-SVG שלהן |
| `T_BASE_SALADS` / `T_EXTRA_SALAD` / `T_BASE_DESSERTS` / `T_EXTRA_DESSERT` / `T_BASE_PASTAS` / `T_EXTRA_PASTA` / `T_PASTA_UP` | תמחור הטאבון |
| `EREV_CHAG` | ערבי חג — משפיע על שעות האיסוף במגשים |
| `ORDER_LABEL` | שם הקטגוריה בסיכום ובוואטסאפ |
| `SALADS` / `CHALLAHS` / `WINES` / `CAKES` / `PROTEINS` | רשימות בחירה |
| `CHEF_PHONE` / `FRUIT_PHONE` / `CHEF_PRICES` / `TABOON_TIERS` | טלפונים ומחירים |

**כלל:** גרש בתוך טקסט חייב להיות `׳` ולא `'`, וגרשיים `״` ולא `"` —
אחרת מחרוזת ה-JS נשברת.

**האימוג׳י בשמות הקטגוריות מסונן אוטומטית** במגש המכסים (`noEmoji()`).

---

## תמחור

### טאבון · הכל לסועד

בסיס לפי `TABOON_TIERS` (עד 15 → 220, עד 20 → 200, מעל → 180).
מעליו `taboonAddPerHead()` מוסיף:

| פריט | כלול | כל אחד נוסף |
|---|---|---|
| סלטים | 3 | 20 ₪ |
| קינוחים | 2 | 20 ₪ |
| פסטות | 2 | 25 ₪ |
| שדרוג מנת פסטה | — | 15 ₪ |
| מנה ראשונה | — | 25 ₪ |

צ׳יפים עם `data-base` מאפשרים לחרוג מהכמות הכלולה; המונה מציג ״3+2״.
עמוד השדרוגים (t6) מכיל היום רק את המנות הראשונות וארבעת הכרטיסים.

### שעות איסוף · לפי קטגוריה

הכל ב-`getWindows(dateStr)`. `genSlots(windows, step)` מייצר את השעות.

| קטגוריה | שעות |
|---|---|
| מסודרים לשישי | ה׳ 17:00–20:00, ו׳ 9:00–15:00 · שישניצל: ו׳ 11:00–15:00 |
| מארזי חג | ו׳ 9:00–15:00 · `HOLIDAY_SPECIAL` דורס · מארז ילדים 7:00–11:00 |
| מגשי פירות | א׳–ה׳ 7:00–20:00 · ו׳ וערבי חג 7:00–14:00 · שבת וחגים סגור |
| שלישי של קוסקוס | ימי שלישי בלבד, לא בחגים · איסוף 11:30–14:30 · משלוח 12:00–14:00 |

**קפיצות בחירת השעה:** מגשי פירות — חצי שעה (`TRAY_SLOT_MIN`). כל השאר — 10 דקות.
**בקוסקוס חלון השעות תלוי ב-`H.ship`** — ולכן `renderCalendar()` רץ מחדש
כשמחליפים בין איסוף למשלוח.

---

## מה נשאר פתוח

1. **שני מארזים חדשים שהלקוחה ביקשה וטרם סופקו:**
   - ״מארז קוסקוס לפי משקל״ (תחת שישי) — שם, תיאור ומחיר
   - ״מארז קוסקוס + מארז סלטים״ (תחת ספיישל) — אותו דבר

   בנוסף **רשימת `EREV_CHAG`** נגזרה מתאריכי 2026 שכבר היו בקובץ ולא אושרה (⚠ בקוד).

2. **חיבור הריפו לנטליפיי** (Link repository) — כדי שכל push יפרסם לבד.

3. **שם הריפו הוא `ShakedLilosPrivetChef`** עם שגיאת כתיב (Privet במקום
   Private). כותרת האתר כבר תוקנה ל-`Shaked Liloz • Private Chef`.
   הועלה ללקוחה, טרם הוכרע.

4. **כתובת האתר בנטליפיי היא `shaked-liloz-orders`** ולא תואמת לשם החדש.
   שינוי ישבור את הקישור הקיים. הועלה ללקוחה, טרם הוכרע.

5. **התיאורים של ארבעת מגשי הפירות נכתבו על ידי Claude**, לא על ידה.
   היא נתנה שמות ומחירים בלבד (300 / 300 / 400 / 450 ₪).

6. **שליחה אוטומטית לוואטסאפ.** ״סיום ושליחה״ מציג רק את מסך האישור,
   לבקשת הלקוחה, עד שיהיה שרת. המקום שבו השליחה תיכנס מסומן בהערה ב-`next()`.

7. **תמונות המארזים.** `.box-thumb` מציג מסגרת מקווקוות עם ״תמונה בקרוב״.

8. **תאריכי 2026 מקודדים** — `HOLIDAY_SPECIAL`, `CHEF_BLOCKED_RANGES`, `EREV_CHAG`.

9. **שלושה לוחות שנה כמעט זהים** (`renderCalendar` / `renderFCalendar` /
   `renderTCalendar`). כדאי לאחד — לא דחוף.

10. **`עריכת-טקסטים.html` לא עודכן** ומתאר את המבנה הישן.

11. **הספרות ב-Karantina דקורטיביות מאוד** — `0/5` נקרא כמעט כמו אותיות.
    הועלה ללקוחה, לא הוכרע.

---

## איך לעבוד כאן

- **לבדוק כל שינוי בדפדפן אמיתי, בשני מצבי התצוגה.** באגים של מרכוז,
  `z-index`, `scroll-snap` ו-`backdrop-filter` התגלו רק ברינדור אמיתי.
- **לוודא שהדף באמת נטען מחדש.** חלונית התצוגה לפעמים מתעלמת מ-`navigate`
  ומחזירה את הדף הישן עם ה-state הקודם. לבדוק `performance.now()` —
  אם הוא גדול, לא היה reload והמדידה חסרת ערך. **הטעה כבר כמה פעמים.**
- **`getComputedStyle` מיד אחרי החלפת `data-theme` מחזיר ערכים ישנים.**
  למדוד בקריאת כלי נפרדת, אחרת מקבלים את הצבעים של המצב הקודם.
- **מדידה אחת שנפלה טוב אינה הוכחה.** באג הקרוסלה ״תוקן״ פעמיים על סמך
  מדידה בודדת שנפלה בתזמון הנכון, וחזר.
- **בדיקת ריחוף לא עובדת באמולציית נייד** — היא מתרגמת עכבר למגע.
  לעבור ל-desktop preset לפני בדיקת `:hover`.
- **קואורדינטות של `hover`/`click` הן במרחב הצילום ולא ב-CSS.** להכפיל לפי
  היחס בין גודל הצילום לגודל ה-viewport.
- **עריכות דרך רג׳קס מסוכנות בקובץ הזה.** להעדיף התאמות מחרוזת מדויקות,
  ואחרי כל עריכה: `node --check` על ה-JS + ספירת רכיבים בדפדפן.
  את ה-JS מחלצים כך:
  ```
  python3 -c "import re;s=open('chef-questionnaire.html',encoding='utf-8').read();print(re.findall(r'<script>(.*?)</script>',s,re.S)[-1])" > /tmp/app.js && node --check /tmp/app.js
  ```
- **גיבוי לפני עריכה גדולה** (`cp chef-questionnaire.html chef-questionnaire.backupN-...html`).
- **הלקוחה עורכת טקסטים בעצמה בקובץ.** לפני עריכה — לקרוא את המצב הנוכחי
  ולא להסתמך על טקסט שמופיע כאן. **אסור לשנות טקסטים שהיא כתבה.**
- **כשמוסיפים טקסט חדש — לומר לה במפורש שהוא נכתב על ידך.**
- **סבבי בחירה מוצגים כקובץ HTML נפרד** (`options-round*.html`) עם אפשרויות
  חיות בשני מצבי התצוגה. זו שיטת העבודה שהשתרשה כאן והיא עובדת טוב.

### לפני סיום כל משימה

```
./build.sh && git add -A && git commit && git push origin main
```

---

## קבצים בתיקייה

| קובץ | מה |
|---|---|
| `chef-questionnaire.html` | **האתר עצמו · מקור האמת** |
| `build.sh` | בונה את `site/` מהקובץ הראשי |
| `netlify.toml` | `command = bash build.sh`, `publish = site` |
| `static/_headers` · `static/robots.txt` | נכנסים ל-`site/` בבנייה |
| `site/` | תוצר בנייה — מוחרג מגיט |
| `עריכת-טקסטים.html` | מדריך עריכה ללקוחה — **מיושן** |
| `chef-questionnaire.backup7-pre-nav-20260823-1249.html` | **לפני מבנה חמשת הכרטיסים** |
| `chef-questionnaire.backup6-pre-hours-20260806-1437.html` | לפני שעות האיסוף |
| `chef-questionnaire.backup5-pre-hub-20260806-1320.html` | **לפני מבנה שלוש הרמות** |
| `chef-questionnaire.backup4-pre-theme-20260803-1118.html` | לפני עבודת הערכות |
| `chef-questionnaire.backup3-20260802-2246.html` · `.backup2` · `.backup` | ישנים |
| `options-round3.html` | עכבר · כפתור · מעברים. **הוכרע** |
| `options-round4-cursor.html` | סבב עכבר שני. **הוכרע — ואז הסמן הוסר לגמרי** |
| `options-round5-pasta-icons.html` | אייקוני פסטה. **הוכרע — ערכה ג׳** |
| `light-options*.html` · `light-glass.html` · `button-options.html` | סבבים ישנים |
| `cover-options.html` · `step-options*.html` · `tile-wow.html` · `box-options.html` | סבבים ישנים |


---

## מערכת ההזמנות · תוכנית ממתינה לאישור

**23.8.26.** הלקוחה ביקשה צד ניהולי, הזמנות אונליין לקוסקוס ולשישניצל,
**ואפליקציה בחנויות** (App Store + Google Play).

**⚠ הבהרה חשובה מהלקוחה:** בתחילה היא נתנה גישה לריפו של הגחנון
(`shakedgoren/Order_management_back` / `_front`) ואמרה ״לבנות באותה הצורה״.
בהמשך הבהירה: **״הגחנון לא מעניין… אין קשר בין השתיים רק לעיצוב״.**
כלומר — השראה עיצובית בלבד, **לא תבנית ארכיטקטונית**. המערכת נבנית מאפס.

**ההחלטות שהתקבלו:**
1. **הכל עובר ל-React** — השאלון הסטטי מוחלף, רק אחרי שוויון תפקודי מלא.
2. **מערכת עצמאית** — לא נגזרת מפרויקט קיים.
3. **אפליקציה בחנויות** — לא PWA. ההמלצה: **Expo** (React Native), שמייצר
   מקוד אחד iOS, אנדרואיד ואתר.
4. **תוכנית כתובה קודם** — אין קוד עד אישור.

**התוכנית המלאה:**
https://claude.ai/code/artifact/f3e8c4a2-8755-428b-9fe1-bf2c219b0da6

**עלות משוערת:** ~300 $ בשנה הראשונה, ~270 $ אחר כך (שרת, בסיס נתונים,
99 $ לשנה לאפל, 25 $ חד־פעמי לגוגל).

**הערכת זמן — היזהר:** בשלב מסוים העריכ שזה ״ימים ולא שבועות״ בהנחה שזה
פורט מהגחנון. **ההנחה הזו בוטלה.** בנייה מאפס + אפליקציה בחנויות +
הגירת 3,800 שורות = פרויקט משמעותי. אל תבטיח לוחות זמנים קצרים.

**התשובות שהתקבלו (23.8.26):**

| מה | נקבע |
|---|---|
| כמויות שלישי | 100 מנות קוסקוס — 30 עוף, 30 מפרום, 40 צמחוני |
| כמויות שישי | 50 חלות שישניצל |
| חלון הזמנות | **ידני** — שקד פותחת וסוגרת מהצד הניהולי. אין סגירה אוטומטית. |
| תשלום | ביט · פייבוקס · אפל פיי · מזומן עם פיקדון אשראי |
| ביטול מאוחר | פחות מ-12 שעות לפני האיסוף → חיוב 30% מהעסקה |
| קבלות | אוטומטיות, נשלחות לוואטסאפ |
| ניהול | שני משתמשים — שקד ובעלה |

**עמלת אפל — נבדק ואומת:** אפל גובה **0% על מוצרים פיזיים ושירותים בעולם
האמיתי**, ואוכל נכלל בזה. יתרה מזו, אפל **אוסרת** להשתמש ב-In-App Purchase
עבורם. אפל פיי היא אמצעי תשלום ולא ערוץ של אפל — גם היא בלי עמלה.
נשארת רק עמלת הסולק, ~2–3%.

**שתי בדיקות פתוחות לפני שבונים את הפיקדון:**
1. כמה זמן עיכוב אשראי מחזיק אצל הסולק (3–30 יום לפי הספק). אם הזמנות
   נפתחות מוקדם מדי, העיכוב עלול לפוג לפני האיסוף.
2. דמי הביטול של 30% — לאישור רואת חשבון. אוכל = טובין פסידים ולכן פטור
   מזכות הביטול הרגילה, אבל התנאי חייב להיות מוצג ומאושר לפני ההזמנה.

**מבנה האפליקציה (23.8.26):**

```
מסך פתיחה · מי אנחנו · מה אנחנו מציעים
        ↓
כניסת מנהלת          כניסת לקוחה (חדשה/קיימת)
   ↓                        ↓
הצד הניהולי            ביצוע הזמנה
                            ↓
                    ההזמנות שלי → "הזמיני שוב"
```

**היקף v1: כל שבע הקטגוריות, כולל שף וטאבון.** כלומר הגירת השאלון
היא חלק מהגרסה הראשונה ולא שלב אחרון. הלקוחה בחרה בזה במודע אחרי
שהוצגה לה החלופה המצומצמת.

**"הזמיני שוב" — כלל עיצוב שנקבע:** הכפתור **ממלא מראש** הזמנה חדשה
ומסמן מה השתנה (מנה שאזלה / ירדה מהתפריט / מחיר שהתעדכן / יום שלא
נפתח). **אף פעם לא לחייב במחיר ישן ולא להשמיט מנה בשקט.**

**האתר הסטטי לא רלוונטי** — לא עולה לאוויר, לא מתוחזק, נשאר על הדיסק.
חיבור הנטליפיי ירד מהשולחן.

**שבעה שלבים.** TestFlight כבר בשלב 1 כדי לבדוק את צנרת הפרסום מוקדם
בלי לחכות שהאפליקציה תיגמר. ההגשה האמיתית בשלב 6.

**דלת יציאה ששווה לזכור:** אם לוח הזמנים נמתח, אפשר להגיש לחנות אחרי
שלב 4 עם קוסקוס ושישניצל בלבד ולהוסיף את השאר בעדכון.

**עדיין פתוח:** שם האפליקציה והאייקון, טקסט מסך הפתיחה (שקד תחליט
אחרי שתראה את המסך — מתחילים מעמוד האודות הקיים), בחירת סולק,
ובחירת ספק קבלות.


### מודול לוגיסטיקה וכספים (נוסף 23.8.26)

נבנה **ביחד עם הצד הניהולי** — שלב 3, צמוד לשלב 2.

**שתי טבלאות מלאי נפרדות · אסור לערבב:**

| | מלאי לוגיסטי (`Supply`) | מלאי מכירה (`ServiceDayItem`) |
|---|---|---|
| מה | אריזות, יבשים, ציוד | מנות מוכנות למכירה |
| עדכון | **ידני בלבד** | נקבע בפתיחת יום, יורד עם כל הזמנה |
| קשור למכירות | לא | כן — הוא התקרה |

**עלות המנה:** שדה `MenuItem.cost` שהלקוחה מזינה ומעדכנת ידנית.
**לא מתכונים ולא BOM** — הוחלט במפורש, כי מתכונים מתנתקים מהמציאות
בגלל פחת והגדשות. מזה נגזר רווח למנה, להזמנה ולחודש.

**⚠ מע״מ — כלל מחייב בכל תצוגה כספית:** המחירים ללקוחות כוללים מע״מ.
מתוך ~400,000 ₪ מחזור שנתי, ~61,000 ₪ אינם של העסק. **כל מספר כספי
חייב להופיע לפני ואחרי מע״מ**, גם בהכנסות וגם בהוצאות (מע״מ תשומות
על קניות חוזר לעסק). דוח שמראה מחזור ברוטו בלבד מטעה בכ-18%.

**זהו דוח ניהולי, לא ספרי הנהלת חשבונות.** לא מחליף את רואת החשבון.

**מודלים חדשים:** `Supply`, `ShoppingList` + פריטים, `Expense`.
רשימת קניות שמסומנת כנקנתה יוצרת `Expense` אוטומטית.

### הזמנות וואטסאפ בצד הניהולי

הלקוחה ממשיכה לקבל הזמנות בוואטסאפ. בהזנה ידנית המערכת **מזהה לקוחה
קיימת לפי הטלפון או פותחת חדשה, ושולחת לה הודעה עם פרטי ההזמנה.**

**זה אומר שהצד הניהולי מייצר ערך מהיום הראשון** — לפני שלקוחה אחת
הורידה את האפליקציה. שווה לזכור אם צריך לקצר לוחות זמנים.

**לשים לב:** התאמה לפי טלפון דורשת נרמול פורמטים
(050-1234567 / 0501234567 / +972501234567).

**שמונה שלבים בסך הכל.** ראה סעיף 08 בארטיפקט.


### לקח בדיקה חדש · 23.8.26

**`getComputedStyle` מחזיר ערכים ישנים גם בקריאת כלי נפרדת.** המסמך הזהיר
מזה, אבל התופעה גרועה יותר ממה שתועד: `body` התעדכן נכון ל-`#0B111C`
בזמן ש-`.chip` עדיין החזיר את הערך של המצב הבהיר. כמעט "תיקנתי" באג
שלא היה קיים.

**הדרך היחידה שעבדה:** לשמור את הערכה ב-`localStorage.shaked_theme`,
לטעון את הדף מחדש, ורק אז למדוד. אז המספרים נכונים.

**וגם:** בבדיקות שמדמות לחיצות, `paintSchnitzel()` בונה מחדש את כל ה-DOM,
ולכן צמתים שנשמרו במשתנה מתיישנים אחרי הלחיצה הראשונה. חייבים לשאול
מחדש את ה-DOM לפני כל לחיצה, אחרת נראה שהסטפר לא עובד.

### מצב פאנל הדפדפן

בסשן של 23.8.26 **לכידת הצילומים בפאנל התקלקלה** והחזירה תמונות של
36×78 פיקסלים. לשונית חדשה ושינוי גודל לא פתרו. האימות נעשה ברמת DOM
בלבד. אם זה קורה שוב — לא לבזבז זמן על ניסיונות חוזרים.


---

# עיצוב האפליקציה · 25.8.26

**זה הפרויקט הפעיל.** השאלון הסטטי לא בתמונה (ראה למעלה), והעבודה עברה
לעיצוב אפליקציית הנייד.

## הארטיפקטים

| מה | קישור |
|---|---|
| **העיצוב הפעיל** · 4 מסכים | https://claude.ai/code/artifact/1f045445-e886-46ce-b3b3-8082b95742a3 |
| תוכנית המערכת | https://claude.ai/code/artifact/f3e8c4a2-8755-428b-9fe1-bf2c219b0da6 |
| ערכות צבע (סבב סגור) | https://claude.ai/code/artifact/acf9abd9-eb90-49c1-b7b4-c110dc8df4f8 |
| כיווני עיצוב (סבב סגור) | https://claude.ai/code/artifact/31e71afa-62c1-43d0-8820-78bd442ac047 |
| דף השוואת סולקים | https://claude.ai/code/artifact/a6d3ab36-b748-4648-9cc7-505f411a0a5d |

## קבצי המקור

`design/app/` — **קבצי העבודה של העיצוב הפעיל**, תשעה עשר ארטבורדים.

**צד הלקוח · תשעה:**

| קובץ | מה זה |
|---|---|
| `Guest.dc.html` | דף הבית לפני התחברות · עם אודות וגלריה, בלי נאב בר |
| `Main.dc.html` | דף הבית אחרי התחברות · סטטוס המכירה, קרוסלה, הזמנה אחרונה |
| `Login.dc.html` | התחברות · מתג כניסה/הרשמה, כניסה עם גוגל |
| `Order.dc.html` | קוסקוס · סגול |
| `Schnitzel.dc.html` | שישניצל · כחול |
| `Boxes.dc.html` | מארזי ספיישל · ירוק |
| `Fruit.dc.html` | מגשי פירות · ורוד |
| `Chef.dc.html` | פינת השף · כתום · ארוחת שף ועמדת טאבון |
| `MyOrders.dc.html` | ההזמנות שלי · מצב ריק, תצוגה מקדימה ופתיחה בלחיצה |
| `Profile.dc.html` | אזור אישי · פרטים, כתובת, אבטחה |

**הצד הניהולי · תשעה:**

| קובץ | מה זה |
|---|---|
| `Admin.dc.html` | דשבורד · בנטו מחובר לכל תשעת המסכים, פתיחת מכירה, דיאגרמות |
| `AdminOrders.dc.html` | הזמנות · סינון לפי מצב, קידום, הזמנה ידנית |
| `AdminBoard.dc.html` | **לוח מכירה לאייפד · 1180×820 לרוחב** |
| `AdminDays.dc.html` | ימי מכירה · לוח זמינות, מכסות, פתיחה להזמנות |
| `AdminMoney.dc.html` | כספים · מחזור, הוצאות, רווח ומע״מ |
| `AdminShopping.dc.html` | קניות · רשימה שסגירתה יוצרת הוצאה |
| `AdminStock.dc.html` | מלאי · שתי טבלאות נפרדות |
| `AdminCustomers.dc.html` | לקוחות · חיפוש, סינון, כרטיס נפתח |
| `AdminMenu.dc.html` | תפריט · מחיר, עלות ורווחיות למנה |

`design/` ו-`design/palettes/` מכילים סבבים סגורים ששווה לשמור להשוואה.
תוצרי ה-seed (`*-glass-app.html` וכו׳) מוחרגים מגיט — הם נבנים מחדש.

## ⚠ תהליך העבודה עם הקנבס · הכי חשוב בקובץ הזה

**שקד עורכת את העיצוב ישירות בקנבס ושומרת.** מי ששומר אחרון מנצח, ואין מיזוג
אוטומטי. **פעם אחת בסשן הזה פרסמתי ודרסתי את העריכות שלה.**

**הנוהל המחייב לפני כל פרסום:**

```
1. WebFetch <artifact url>              ← מחזיר נתיב לקובץ שנשמר
2. seed-canvas.mjs --extract <קובץ> --to <תיקייה ריקה חדשה>
3. diff מול design/app/                 ← לראות מה היא שינתה
4. cp <החילוץ> design/app/              ← לאמץ את הגרסה שלה כבסיס
5. לערוך · ולאמת עם grep שהשינויים שלה שרדו
6. seed + --check + Artifact publish
```

**אימות חובה בשלב 5.** בפעם שנכשלתי, ה-`cp` לא תפס ופרסמתי קובץ ישן.
תמיד לוודא ב-grep שהעריכות שלה קיימות בקובץ שממנו מפרסמים, **ולהשוות
גדלים** — קובץ ממוזג חייב להיות גדול מהמקור.

**פקודות ה-seed** (הנתיב לסקילדים משתנה בין סשנים — להריץ `/design` לחילוץ מחדש):
```
SK=<base>
node $SK/seed-canvas.mjs --template $SK/payload.template.html \
  --out shaked-liloz-glass-app.html --title "אפליקציית שקד לילוז" \
  $(for f in Main Guest Login Admin AdminOrders AdminBoard AdminDays AdminMoney \
        AdminShopping AdminStock AdminCustomers AdminMenu \
        Order Schnitzel Boxes Fruit Chef MyOrders Profile; do \
      printf -- "--artboard design/app/$f.dc.html "; done) \
  --canvas design/app/canvas.json
node $SK/seed-canvas.mjs --check shaked-liloz-glass-app.html
```

**במקום WebFetch משתמשים ב-`Artifact` עם `action: "read"`** — הוא מחזיר נתיב
לקובץ מלא שנשמר, וממנו מריצים `--extract`.
פרסום: `Artifact` עם `contract: "0.1.31"` ובלי `capabilities` (ברפרסום).

## שפת העיצוב שנקבעה

**פסטל רב־גוני רך** — לבנדר, מנטה, אלמוג ותכלת יחד, על רקע `#FAF7F5`.
הכיוון נבחר מרפרנס שהלקוחה שלחה (אפליקציה בשם OurBeat). **לקחנו כיוון צבע
בלבד, לא עיצוב** — הסרטים, הלב והמסלול הממוספר שם הם זהות של אפליקציה אחרת.

**כל נושא צובע עשרה דברים:** הכרטיסייה · הכדור · החרוזים · המסגרת המקווקוות ·
המספר · הכיתוב · הנקודה הפעילה · הקו התחתון · הכפתור · כפתור הבית.

| הנושא | גוון | rgb |
|---|---|---|
| קוסקוס | `#7B5CBC` סגול | 123,92,188 |
| שישניצל | `#416D9E` כחול | 65,109,158 |
| ספיישל | `#437C59` ירוק | 67,124,89 |
| פירות | `#B04A76` ורוד | 176,74,118 |
| שף וטאבון | `#A85A28` כתום | 168,90,40 |

**הפלטה הוחלפה 27.8.26 לבקשת הלקוחה.** כל חמשת הגוונים עוברים 4.5:1 על
לבן ולכן משמשים גם כטקסט — אין יותר צורך בגרסת־טקסט כהה נפרדת.
לכיתוב על משטח בגוון הנושא יש גוון עמוק:
קוסקוס `#43307A` · שישניצל `#2B4A6E` · ספיישל `#2C5A3E` ·
פירות `#7A2E4E` · שף `#7A3D18`. כולם מעל 8:1 על לבן.

**מה שלא זז:** הכפתורים נשארו סגול בהיר עם שזיף, הירוק בצד הניהולי הוא
סמנטי (מתג פתוח, רווח) ולא קטגורי, והסגול בגרף המחזור ובצ׳יפי הניווט הוא
גוון האפליקציה הכללי.

**טיפוגרפיה:** Assistant בלבד, משקלים 200–700. כותרות 600, תיאורים 300.
Rubik נוסה ונזנח — כבד מדי לאווירה.

**כפתורים:** סגול בהיר `#D2C2F2` → `#B9A4E4` עם כיתוב שזיף `#43307A`.
**לא לבן** — הניגודיות נופלת ל-1.9:1. עם השזיף זה 4.9:1.

**משטחים תלת־ממדיים:** כדור נושא + סרט קשת **רק במסך הבית**. בשאר המסכים
התלת־ממד בפקדים בלבד (פלוס כדור מבריק, מינוס כרית לבנה, וי כדור קטן).
הצ׳יפים והשורות נשארים שטוחים בכוונה — ברק על הכל מוזיל את המראה.

## מבנה מסך הבית

אינטראקטיבי. קרוסלה של 5 כרטיסים על מסילה + שורת 5 עיגולים עם אייקונים
מותאמים. לחיצה על עיגול מזיזה את המסילה (560ms, `cubic-bezier(.22,.9,.28,1)`),
הכרטיס הנכנס גדל ומתבהר והיוצא מתכווץ ל-0.35.

**התמונות:** הלקוחה תעלה תמונה לכל נושא. המקום מסומן במסגרת מקווקוות בתוך
הכדור שבכרטיס. **האייקונים בשורה התחתונה נשארים אייקונים** — התמונה מופיעה
רק בכרטיס. הומלץ: רקע בהיר אחיד, ריבועי, ~600×600.

**בלוק האודות** יושב ב-`position: absolute; left: 1px; top: 64px` — במקום
שהלקוחה סימנה בציור ידני. כפתור ״לסיפור המלא״ פותח `sc-if` עם כל שבע
הפסקאות מ-`ABOUT_BODY` ואיקס בצד שמאל.

## מגבלת הפורמט שנתקלנו בה

**אין תמיכה בתכונות `style-*` נפרדות.** ערך דינמי בסגנון נכתב כאינטרפולציה
בתוך מחרוזת הסגנון: `style="... transform: {{ c.scale }};"`. זה עובד; ניסיון
ראשון עם `style-transform="{{ }}"` נכשל.

## מצב חמשת מסכי הקטגוריות · 31.8.26

כל חמש הקטגוריות עוצבו. שפת המסך אחידה: כותרת + חץ חזרה בפינה, תיאור ממורכז,
הבחירות, שורת `סה״כ : X ₪` + `המשך` מעל הנאב בר, ואז הזרימה
(איך מגיע → שעה → כתובת → תשלום → סיכום → דף אישור עם קונפטי).

### קוסקוס · `Order.dc.html`
שלוש מנות עם מונה, שלוש תוספות בשלוש עמודות. מינימום 4 מנות למשלוח
עם הודעה במעלה המסך. איסוף 11:30–14:30, משלוח כל 20 דק׳.

### שישניצל · `Schnitzel.dc.html`
מתג ״לפי יחידה / מארז״. סוגי החלה בשתי עמודות עם תמונה מעל בשני המצבים.
לחיצה על סוג פותחת חלונית תוספות. שלושה קוקוטי רוטב ב-3 עמודות.
כנפי עוף במתנה · ״ברוטב מתחלף בכל שבוע ע״פ בחירות הסקר השבועי באינסטגרם.״

### מארזי ספיישל · `Boxes.dc.html`
מסך קטגוריות (בלי סה״כ) → מסך פנימי לכל אחת מהשבע.
מנוע מקטעים גנרי: `title · note · price · fine · chips · grid · cards · pair ·
count · row · stepper · text · images · hidden`.
תמחור: ״קחו כמה שבא לכם״ 25 ₪ לחלה וזוג ב-44, סלטים 89 ₪ לק״ג לפי מ״ל
(מינימום 500, קפיצות 100). ״חלה לכל אירוע״ 14 ₪ ליח׳ ומעל 50 יח׳ 12 ₪,
ועוד תוספת לפי סוג האירוע (ראש השנה +6, הפרשות חלה +6, פינוק לגן +4).
מפרום מוסיף 20 ₪ בחגיגה·עיקרית (339/359) וגם בהכל עלינו (499/519 — היסק שלי).
כרטיסי סוג האירוע: תמונה ממלאת עם שכבה ירוקה בהירה `#C6E4D3`, שניים בשורה.

### מגשי פירות · `Fruit.dc.html`
ארבעה מגשים בשתי עמודות, תמונה בגובה 118px, התיאור וכמות הסועדים בשתי
שורות (פיצול אוטומטי על `•`). פתוח כל השבוע — בכותרת שעות במקום תאריך.
איסוף 7:00–20:00, משלוח בקפיצות חצי שעה. הטלפון של מיכל גורן הוא קישור
`tel:` אמיתי.

### פינת השף · `Chef.dc.html`
תפריט עם מתג (ארוחת שף / עמדת טאבון), קרוסלת תמונות בהנפשת CSS, תיאור,
וכפתור ״בחר מסלול״.

**ארוחת שף · אשף בן שישה שלבים** עם חץ חזרה בכל שלב:
1. סועדים (2–16) · ציר · סגנון · מסלול
2. מה הטעמים המועדפים עליכם
3. מה לא ייכנס לתפריט
4. פרטים נוספים (המטבח במקום + משהו שחשוב לכם שיהיה)
5. אפשר גם להעלות הילוך
6. השארת פרטים לחזרה → ״סיום ושליחה״ → סיכום מלא

**כללי הנעילה:** הסגנון והמסלול אפורים עד שנבחר ציר. חלבי (צמחוני/דגים)
פותח איטלקי ואסייאתי בלבד; בשרי פותח את כל הארבעה. שינוי ציר מאפס סגנון
שכבר אינו חוקי. שאלת עשיית הבשר מופיעה רק בבשרי; שאלות הדגים רק בחלבי·דגים.

**תמחור:** ₪ לסועד לפי משפחה (milk/meat) × מדרגת סועדים (2-4 / 5-11 / 12-16)
× מסלול. הטבלה בקוד היא זו שבאתר הישן.

**עמדת טאבון · שישה שלבים** (נבנה 31.8.26 מהזרימה של השאלון הישן):
1. סועדים (10–25) + **סלטים 0/3**
2. **פסטות 0/2** · הרטבים במסך, ולחיצה על רוטב פותחת חלונית עם שבע
   הצורות ומתחתיהן חמשת השדרוגים. **או צורה או שדרוג, לא שניהם.**
3. **קינוחים 0/2**
4. אפשר גם להעלות הילוך · מנות ראשונות + ארבעה כרטיסי שדרוג
5. אלרגיות, מגבלות תזונתיות, מה לא על הפיצה, ומקום הצבת העמדה
6. השארת פרטים · שם, טלפון, תאריך, שעות, כתובת

**תמחור הטאבון · `TABOON_TIERS`** — עד 15 → 220, עד 20 → 200, עד 25 → 180
₪ לסועד. **המחיר הקבוע של 55 ₪ שהיה בעיצוב היה שגוי והוסר.**
מעליו לסועד: סלט נוסף 20 · קינוח נוסף 20 · פסטה נוספת 25 ·
שדרוג מנת פסטה 15 · מנה ראשונה 25 · מנות דג 55 · שולחן קינוחים 40 ·
ממשותף לאישי 20. עיצוב שולחן הוא סה״כ ולא לסועד: 250 / **350** / 450
לפי אותן מדרגות (350 הוא מספר שהוצע ואושר 31.8.26).

**באג שתוקן:** קינוח רביעי ומעלה לא חויב כלל — לא היה `DESSERT_EXTRA`.

**דף האישור:** ״ההזמנה נשלחה״ · ״אישור סופי יישלח בהמשך״ · ואז
**שורות מתומחרות** (המסלול והשדרוגים, כל אחד ״עבור N סועדים״),
הפירוט המלא, **ורק בסוף הסה״כ** עם ״X ₪ לסועד״ מתחתיו.
הסדר הזה נקבע 31.8.26 — הסה״כ ירד מלמעלה לסוף.

**מגשי פירות · תיקון 31.8.26:** אזור הגלילה התחיל ב-`top: 88px`, ערך
של כותרת בת שתי שורות. שם יש שלוש, ולכן היה חופף. הועבר ל-`108px`.

## פקדים · הסטנדרט שנקבע 31.8.26

**בחירה מרשימה · הסטנדרט שנקבע 31.8.26.** כשכל אפשרות נבחרת פעם אחת
בלבד — **אין כמות ואין + ו־−.** לחיצה מסמנת, לחיצה נוספת מבטלת.
**אין סימן וי** — הסימון הוא הרקע והמסגרת בלבד. הכיתוב ממורכז.
המכסה מוצגת בכותרת כמונה חי (״סלטים 0/3״) **שממשיך לעלות מעל המכסה**.
בהגעה למכסה נפתחת הודעה **פעם אחת בלבד לכל קבוצה** (`state.seen`):
״בחרתם את הכמות המותרת, / כל בחירת X נוסף בתוספת N ש״ח לסועד.״
מיושם ב-`kind: 'multicap'` ו-`kind: 'sauces'`.

**כל כפתורי + ו־− באפליקציה שטוחים, 32px, אייקון 13px.** הפלוס בגוון הקטגוריה
ברקע רך (`rgba(hue,0.13)`), המינוס `rgba(130,112,162,0.09)`. התלת־ממד
הרדיאלי הישן הוסר מקוסקוס ומשישניצל.
**חריג יחיד:** שורת החלות במארזי ספיישל היא 26px כי היא בשלוש עמודות צרות.

**מיקום:** `flex-direction: row-reverse` — מינוס בשמאל, פלוס בימין.
**חריג:** בסלטים, במגשים ובכרטיסי המנייה החדשים הפלוס בקצה ימין והמינוס
בקצה שמאל עם הכמות באמצע מתחת לתיאור.

**כותרות ותיאורים ממורכזים** בכל מסכי הקטגוריות. רווח גדול לפני כותרת
(`padding-top: 12px`) וקטן אחריה, כדי שכל כותרת תיצמד לתוכן שלה.

## הפלטה · קרקע נקייה · 1.9.26

הוחלף מסבב של שבע פלטות. הקרקע עברה מ-`#FAF7F5` ל-**`#FCFBFB`**, ושלוש
הילות הרקע ירדו לכשליש (0.22/0.20/0.18/0.16/0.15 → 0.08/0.07/0.06/0.05).
`pageWash` ירד מ-0.14 ל-0.08.

**הוחל על כל המסכים**, לא רק על הבית — אחרת המעבר בין מסכים מראה קפיצה
בגוון הרקע. **המבנה של דף הבית לא שונה** · נבחנו חמישה כיוונים חלופיים
וחמש וריאציות, והוחלט להישאר עם הקרוסלה הקיימת ולשנות רק צבע.

## מסכי צד הלקוח שנוספו · 1.9.26

**`MyOrders.dc.html` נבנה מחדש.** היה שריד מסבב שננטש — Rubik בשמונה
מקומות ורקע `#FAF5F3`. עבר למנוע נתונים (`ORDERS` + `sc-for`) כמו כל
מסך אחר. הכרטיס הסגור מציג רק סטטוס, כותרת ומחיר; לחיצה פותחת פירוט
מלא. בהזמנה הפעילה שעת האיסוף גלויה גם סגור. מצב ריק עם המשפט של שקד.

**`Profile.dc.html`** · תמונה, פרטים אישיים, כתובת, אבטחה.
**הכתובת היא קופסה אחת** עם השלמה אוטומטית מ-`STREETS`; עיר מחוץ לטווח
מציגה בסגול ״אין משלוחים לאיזור הזה״. כפתור ״שמור״ אפור ולא לחיץ עד
שיש שינוי, ואחרי שמירה הערכים הופכים לבסיס החדש.

**`Login.dc.html`** · מתג כניסה/הרשמה, כניסה בטלפון או במייל,
**״המשך עם Google״** בשני המצבים, ואישור תנאים בהרשמה.

**פקד ההזדהות בכותרת** · ב-Guest, Main, MyOrders ו-Profile.
אין פרופיל מחובר → אייקון אדם שמוביל להתחברות. יש → אייקון יציאה
עם דיאלוג אישור. מתג `loggedIn` בפאנל התכונות.

## הצד הניהולי · 1.9.26

**ההחלטות שהתקבלו:**

| מה | נקבע |
|---|---|
| כניסת מנהלת | **אותו מסך התחברות**, ניתוב לפי תפקיד. אין מסך נפרד |
| לוח זמינות | **לוח אחד + חריגה לקטגוריה בודדת** ביום חסום |
| מע״מ | פיצול לפני/אחרי **רק במסכי הכספים** |
| עיצוב שולחן בטאבון | 250 / **350** / 450 לפי מדרגות הסועדים · אושר |

**`AdminBoard.dc.html` · לוח המכירה לאייפד** הוא המסך היחיד לרוחב
(1180×820). שני טאבים איסוף/משלוחים, מיון בשלוש קבוצות צבע — חדשות
בכתום, מוכנות בירוק, נמסרו באפור — ובתוך כל קבוצה לפי שעת איסוף.
עשר עמודות. **לחיצה על תא מעדכנת כמות, והסה״כ, סכום העמודה ומוני
המלאי מתעדכנים באותו רגע.** `CATS` מגדיר את העמודות לכל קטגוריה,
כך שהוספת שישניצל היא רשומה אחת.

**`AdminStock.dc.html` · שתי טבלאות שאסור לערבב.** מלאי מכירה הוא
לקריאה בלבד ומפנה לימי מכירה; מלאי לוגיסטי ידני בלבד עם מינימום
לכל פריט וכפתור שמוסיף את החסרים לרשימת הקניות.

**`AdminMenu.dc.html`** מחזיק את `MenuItem.cost` מהמסמך — העלות
שמוזנת ידנית, וממנה נגזרים הרווח והרווחיות למנה. רווחיות מתחת
ל-40% מסומנת בכתום.

## הזמנה ידנית וימי מכירה · 2.9.26

**`AdminOrders.dc.html` · ההזמנה הידנית נבנית כמו הזמנת לקוחה.**
שדה **שם מלא** ראשון, עם השלמה חיה מתוך `BOOK` (פנקס הלקוחות) —
מקלידים אותיות, נפתחת רשימה של עד ארבע התאמות, ולחיצה על שם מושכת
את הטלפון והכתובת מהרשומה. אם אין התאמה, מקלידים טלפון והשמירה
פותחת ללקוחה כרטיס. `isKnown()` משווה טלפונים מנורמלים (`norm`),
כך ש-`0501234567` מזוהה מול `050-1234567` שבפנקס.

**רק שתי קטגוריות** (`MANUAL_CATS = ['cous','schn']`) — אלה שנמכרות
בימי מכירה. **אין שדה חופשי ״מה הוזמן״ ואין הקלדת סכום:** `MENU`
מגדיר את הפריטים והמחירים לכל קטגוריה, הבחירה נעשית בסטפרים,
ו-`newItemsSum()` / `newShipFee()` / `newTotal()` מחשבים את הסכום.
החלפת קטגוריה מאפסת את הפריטים (`setCat`) כי הרשימות לא חופפות.

**נוסף משלוח לצד האיסוף.** `SHIP_FEE` — 20 ₪ ביבנה, 60 ₪ בשפלה,
ו-`DELIV_MIN_MEALS = 4` חוסם משלוח קוסקוס מתחת לארבע מנות
(תוספות אינן נספרות כמנות). כתובת חובה במשלוח.

**השמירה מוסיפה הזמנה אמיתית לרשימה** — `saveNew()` דוחף רשומה
ל-`st.extra`, ו-`allOrders()` מחבר אותה ל-`ORDERS`. ההזמנות הקיימות
שומרות על האינדקסים שלהן, כך ש-`moved` לא נשבר.

**`AdminDays.dc.html` · שני תיקונים.** (א) החריגה הייתה שבורה:
`hasSale` נגזר מ-`r.sale` בלבד, כך שביום חסום עם חריגה לא הופיעו
מכסות ולא כפתור ״פתוח להזמנות״. עכשיו
`activeKey = r.blocked ? r.except : r.sale`, `setExcept()` מאתחל
מכסות דרך `freshQuota()` בדיוק כמו בחירת יום מכירה, ותא הלוח מקבל
נקודה בגוון החריגה בלי לאבד את האפור. (ב) השישניצל פוצל במלאי
לשני פריטים — **שניצל דק בציפוי פירורי לחם** ו-**פילה עוף בציפוי
טמפורה** — בשמות המדויקים מ-`Schnitzel.dc.html`.

⚠ **טקסטים שכתב Claude במסך ההזמנה הידנית:** ״לקוחה קיימת״,
״לקוחה חדשה · תיווצר לה כרטיסייה עם שמירת ההזמנה״,
״משלוח קוסקוס מתחיל מ-4 מנות״, ״עם השמירה ייפתח ללקוחה כרטיס
במערכת ותישלח לה הודעת וואטסאפ״, ו-״טרם שולם״ בשורת התשלום.

## סבב תיקוני הניהול · 2.9.26

**עשרים ארטבורדים.** נוסף `AdminCosts.dc.html` — עלויות ייצור.

**`AdminOrders.dc.html`** · המינוס והפלוס התחלפו במקומם. השישניצל
בהזמנה ידנית קיבל **מארזים** (200/250) ו-**רטבים בצד** בנוסף לחלות,
ובלוק **״מה נכנס לחלה״** שמופיע רק לסוגים שנמצאים בהזמנה — התוספות
מ-`SCH_TOPS` לפי הסוג, בדיוק כמו במסך הלקוחה.

**`AdminDays.dc.html`** · המכסות בקוסקוס סופרות גם תוספת ירקות, עוף
ומפרום בנפרד; ובשישי של מטעמים נספרים גם מארזי שניצל דק ופילה עוף.

**`AdminMoney.dc.html`** · **אין יותר פיצול מע״מ.** שקד עוסקת פטורה,
וכל הסכומים מוצגים כפי שנגבו ושולמו. **מגשי הפירות ירדו** מהמחזור
ומהפילוח — המחזור החודשי ירד ל-40,900 (וגם בדשבורד).

**`AdminMenu.dc.html`** · המסך **לקריאה בלבד**. אין מתג הדלקה/כיבוי,
אין קטגוריית פירות, ואי אפשר לערוך מחיר או עלות. הכול מגיע ממסך
העלויות.

**`AdminCosts.dc.html` · חדש.** לכל מנה: רשימת המצרכים שנקנו החודש
עם המחירים (`addPart` / `removePart`), כמות המנות שנמכרו, ומחיר
המכירה. מזה נגזרים `batchCost`, `unitCost = אצווה ÷ מנות`, הרווח
והרווחיות — ואלה עוברים למסך התפריט. **תזכורת ב-1 בחודש** מופיעה
בראש המסך ובאריח בדשבורד (תג כתום).

**`AdminShopping.dc.html`** · `PANTRY` הוא פנקס המצרכים מהקניות
הקודמות — הקלדה מציעה השלמות, ובחירה מושכת קבוצה, יחידה ומחיר אחרון.
כל שורה היא **כמות × מחיר ליחידה** והסה״כ מחושב. **סגירת הרשימה**
עכשיו באמת עושה משהו: נרשמת הוצאה, הרשימה נכנסת ל״קניות קודמות״,
ונפתחת רשימה ריקה לתאריך הבא.

**`AdminStock.dc.html`** · המינוס והפלוס התחלפו במקומם. נוסף **פריט
חדש למלאי** (שם, קבוצה, יחידה, כמות ומינימום). במלאי המכירה נוסף
**״ירדו מהמכירה״** — סטפר שמוריד מנות שהתקלקלו או נפלו בלי לגעת
במכסה ובלי לגעת בהזמנות; הבר, ה״נותרו״ והסה״כ מתעדכנים.

**`AdminCustomers.dc.html`** · אנשי הקשר ממוינים **לפי א־ב**
(`localeCompare(he)`). נוסף כפתור **״הזמנות קודמות״** שפותח חלונית עם
ההיסטוריה של הלקוחה, ו**הערה חופשית וניתנת לעריכה** לכל לקוחה (פיצוי,
אלרגיה) שמופיעה כתג כתום על הכרטיס. הכרטיס נפתח מהכותרת בלבד, כדי
שאפשר יהיה להקליד בהערה בלי שייסגר.

**`AdminBoard.dc.html`** · **הפלוס והמינוס ירדו לגמרי.** התא עצמו הוא
שדה — לוחצים על המספר ומקלידים את החדש. העמודות הצרו (68px) והפעולות
קיבלו 242px, כך ש״סמני כמוכנה״ לא עולה יותר על עמודת התשלום. **האיקס
של הביטול נעלם אחרי שההזמנה נמסרה.**

⚠ **טקסטים שכתב Claude בסבב הזה:** ״מה נכנס לחלה״, ״הגיע ה-1 בחודש״,
״זה הזמן לעדכן את עלויות הייצור״, ״מה נקנה החודש״, ״עוד מצרך״,
״עלות למנה״, ״המסך הזה לקריאה בלבד...״, ״מגשי הפירות אינם נכללים
כאן...״, ״קניות קודמות״, ״ירדו מהמכירה״, ״נפילה, שריפה או טעות״,
״פריט חדש למלאי · למשל אחרי שינוי מתכון״, ״הזמנות קודמות״,
״למשל: מגיע פיצוי על ההזמנה שבוטלה״.

⚠ **פרשנות שדורשת אישור:** מסך העלויות בנוי כאצווה חודשית לכל מנה —
מה נקנה עבורה, כמה זה עלה, וכמה מנות יצאו — והחלוקה נותנת את העלות
למנה. אם הכוונה הייתה רשימת קניות אחת שמתחלקת בין כל המנות, המבנה
צריך להשתנות.

## סבב הניהול השני · 3.9.26

**עשרים ואחד ארטבורדים.** נוסף `AdminHistory.dc.html` — היסטוריית קניות.

**`AdminOrders.dc.html`** · כל חלת שניצל נבחרת **בחלונית משלה**
(`SCH_ROLLS` / `openRoll` / `saveRoll`), כי בהזמנה של שלוש חלות כל אחת
מקבלת תוספות אחרות. החלות יושבות ברשימה — לחיצה עורכת, איקס מוחק.
מארזים ורטבים נשארו סטפרים.

**`AdminDays.dc.html`** · הפלוס עבר לימין המספר והמינוס לשמאלו,
ושורת ״סה״כ מנות״ יושבת בתיבה ברוחב 114px — בדיוק מתחת לעמודת הכמויות.

**`AdminMoney.dc.html` / `AdminMenu.dc.html`** · הוסרו הערות השוליים.

**`AdminCosts.dc.html` · נבנה מחדש כעץ.** הכותרת היא ״חישוב עלות
למנה אחת״. כל שורת מצרך היא **מוצר · מחיר · כמות · סה״כ** (מחושב),
עם השלמה מפנקס המצרכים. שלושה מצבי חישוב:
`unit` (עלות ליחידה = מצרכים ÷ כמה יצא), `weight` (עלות ל-100 גרם,
לכל מה שנמכר במשקל), ו-`auto` (העלות נשאבת ממנות אחרות).
**המחיר נקבע כאן ורק כאן** — זה המחיר של כל האפליקציה.

עץ הספיישל: **סלטים** (14 סלטים, כל אחד לפי משקל) · **חלות**
(חלת שישי משפחתית — שומשום/קמח/קלאסית הן אותה חלה ואותו מחיר;
״חלה לכל אירוע · הבסיס״ ומתחתיה ארבעת האירועים ששואבים ממנה) ·
**מארזים מוכנים** (חגיגה בשולחן סלטים — אוטומטי מהחלות ומממוצע
הסלטים; חגיגה בשולחן עיקרית — ידני, כי היא לפי משקל ולא לפי מנה;
הכל עלינו — אוטומטי לגמרי משני אלה, רק המחיר נקבע; טעם של שנה טובה
— ידני, המחיר שונה ל-49 ₪). **מארז פרימיום אינו במסך** לפי בקשתה.

**`AdminShopping.dc.html`** · לכל קנייה **שם ייחודי**: שיוך + תאריך
+ שעה, מהשעון האמיתי. שיוכים: קוסקוס, שניצלים, מארזים, שף, טאבון,
כללי. יחידות: ק״ג · יח׳ · קרטון (צרורות ירדו). השורות צרו לגובה
36px ומציגות **מוצר · מחיר · כמות · סה״כ** עם תיבת סימון 18px.
כפתור ההוספה הוא **+** בלבד, ולידו כפתור שעון להיסטוריה.

**`AdminHistory.dc.html` · חדש.** תצוגה מקדימה: שם הקנייה, מספר
הפריטים בקטן, והמחיר הכולל בצד. לחיצה פותחת את הקנייה המלאה עם
כל השורות. סינון לפי שיוך.

**`AdminStock.dc.html`** · מלאי המכירה נגזר מ-`OPEN_DAYS` — **ימי
המכירה הפתוחים כרגע**. פתוח אחד, מוצג אחד; פתוחים שניים, מוצגים
שניהם בשתי כותרות; אין פתוח, הודעה. הקוסקוס כולל עכשיו את שלוש
התוספות בנפרד. הפלוס עבר לימין. במלאי הלוגיסטי נוספה **מחיקת פריט**.

**`AdminBoard.dc.html`** · הטבלה הודקה (1126px, בדיוק רוחב המסגרת)
ובמקום כפתורי טקסט יש **שלושה אייקונים בכל שורה**: מגש נכנס
(התקבלה), וי בעיגול (מוכנה), ווי כפול (נמסרה). לוחצים ישירות על כל
אחד, קדימה או אחורה. האיקס נעלם כשההזמנה נמסרה.

⚠ **טקסטים שכתב Claude:** ״מה נכנס לחלה הזאת״, ״הוספת החלה״ /
״עדכון החלה״, ״חישוב עלות למנה אחת״, ״נשאב מקטגוריות אחרות״,
״אוטומטי״, ״כמה גרם יצאו״ / ״כמה יחידות יצאו״, ״עלות ל-100 גרם״,
ההערות בכרטיסי החלות והמארזים, ״שיוך הקנייה״, ״היסטוריית קניות״,
״אין קניות בשיוך הזה״, ״אין כרגע יום מכירה פתוח״.

⚠ **מה עוד לא מחווט:** בקנבס כל ארטבורד עומד בפני עצמו, ולכן
מעבר המחיר מ-`AdminCosts` אל התפריט ואל צד הלקוחה **מוצג ולא
מחווט**. שמירת המצרכים מחודש לחודש היא באותה מידה תיאור ולא מנגנון.

## סבב הליטוש · 3.9.26 · BITE and TELL

**שם העסק החליף את השם הפרטי** בכותרות של הבית, הבית־לפני־התחברות
וההתחברות, וגם בשם הקנבס. ⚠ הקנבס דוחה `&` בכותרת, ולכן שם הארטיפקט
הוא **BITE and TELL** ובמסכים עצמם `BITE &amp; TELL`.
״נעים להכיר, אני שקד לילוז״ נשאר — זה השם הפרטי בסיפור, לא שם העסק.

**`AdminCosts.dc.html`** · ״כמה יחידות יצאו״ → **״כמות (יח׳)״ / ״כמות
(גרם)״**, ״מחיר מכירה״ → **״מחיר ליח׳״ / ״מחיר לק״ג״**, שתיהן ממורכזות
מעל התיבה. נוסף **כפתור ייבוא** בראש המסך: בוחרים רשימת קניות
(`importBuy`), וכל מצרך ששמו תואם מקבל את המחיר ששולם בפועל — בכל
המנות בבת אחת, עם הודעה כמה שורות עודכנו. **הכמות לא מיובאת** בכוונה:
קנייה אחת מזינה כמה מנות, והכמות היא של המתכון. במארזים נוספו שורות
**מארז · שקית נשיאה · כרטיס ברכה** (`partsTitle` נהיה ״האריזה
והתוספות״ במנות אוטומטיות), והן נכנסות לעלות.

**`Boxes.dc.html`** · טעם של שנה טובה עודכן ל-**49 ₪** גם בצד הלקוחה.

**`AdminShopping.dc.html`** · ״בשר ועוף״ → **״בשר עוף ודגים״**, ונוספה
קבוצת **״כללי״**.

**`AdminHistory.dc.html`** · שורת הסינון נהייתה גוללת (`overflow-x`)
כך ש״כללי״ לא נחתך. אותו תיקון הוחל גם בקניות, בעלויות ובתפריט.

**`AdminStock.dc.html`** · כפתור הוספת הפריט הוא **+ בראש העמוד**
בצד שמאל, כמו בקניות, ומופיע רק בטאב הלוגיסטי.

**`AdminBoard.dc.html`** · נוסף טאב **״הכל״** שמציג איסוף ומשלוחים יחד.
עמודת השם הצטמצמה ל-220px (ההזמנות נדחקו ימינה אליה) ועמודת הסטטוס
גדלה ל-206px עם פדינג שמאלי — האיקס כבר לא נחתך. שמות המנות בכותרות
כולם בבולט.

**`Guest.dc.html` · ליקוויד גלאס.** שתי הילות אור שנעות לאט מתחת
לזכוכית (`lgDriftA` / `lgDriftB`, 22 ו-28 שניות), שכבת שבירה
דיאגונלית, קו אור בשפה העליונה, וברק שחולף כל 9 שניות (`lgSheen`).
הילת האור נצבעת בגוון הקטגוריה שעל המסך. **הסיפור האישי** נפתח
בפסקאות שעולות בזו אחר זו (`.story`), והשורה ״כי בשבילי, אוכל הוא אף
פעם לא רק אוכל״ **נכתבת אות אחר אות** (`.typing`). הטקסטים שלה לא
נגעו — רק העטיפה.

**`design/app/assets/` · תיקיית התמונות.** ה-README שם מפרט את כל
השמות המדויקים. **`scripts/seed.sh`** אוסף אוטומטית כל תמונה
מהתיקייה ומטמיע אותה — אין צורך לערוך פקודות.
מגבלה: **עד ~70 KB לתמונה**, מעל 2 MB הקנבס מוחק אותה בשמירה.

⚠ **טקסטים שכתב Claude:** ״ייבוא״, ״ייבוא מרשימת קניות״, ״המחירים
ששולמו בפועל יעדכנו כל מצרך תואם״, ״עודכנו N שורות מצרכים״,
״האריזה והתוספות״, ״כמות (יח׳)״ / ״כמות (גרם)״, ״מחיר ליח׳״.

## הסיפור המוקלד והלוגו · 3.9.26

**`Guest.dc.html` · הסיפור נכתב אות אחר אות.** אנימציית הכניסה
המדורגת ירדה — היא קפצה. במקומה `STORY` (מערך של הטקסט שלה, מחולק
לשורות), `openAbout()` מפעיל טיימר של 3 אותיות כל 16ms, `storyBlocks()`
חותך את מה שנחשף עד כה, ו-`tick()` מסנתז קליק קצר ב-Web Audio (בלי
קובץ קול, ובלי להתרסק בדפדפן שחוסם). **כל הטקסט ממורכז**, לכל שורה
`min-height` קבוע כדי שלא תהיה קפיצה, ולחיצה על הסיפור מסיימת את
ההקלדה מיד. ⚠ הטקסט הועתק מילה במילה מהמארקאפ הקודם — לא נוסחה מחדש.
⚠ שני `componentWillUnmount` מוזגו לאחד; הראשון היה מנקה רק את
מאזין ה-resize.

**`Login.dc.html`** · הסכין־מזלג הוחלף ב-`<img src="logo.png">` בתוך
עיגול הזכוכית. `alt=""` בכוונה — עד שהקובץ יעלה, נראית רק הזכוכית
ולא טקסט חלופי שבור.

**`AdminStock.dc.html`** · בהוספת פריט ״יחידה״ → **״כמות״**,
והאפשרויות הן **יחידה · ק״ג · חבילה · קרטון**. `PACKED` מגדיר אילו
מהן הן אריזה שלמה, וכשנבחרת אחת כזאת נפתח שדה **״כמה יח׳ ב…״**
שהוא חובה לשמירה. הכמות באריזה מוצגת בשורת הפריט.

**תצוגת חמשת סגנונות הכותרת** פורסמה כארטיפקט נפרד:
`https://claude.ai/code/artifact/6133d4c3-65b9-4e3e-89cf-6aa42014c6c7`
חמישה טיפולים — נקי ורחב · סריף אלגנטי · קו דק ורחב · מונוגרמה ·
זכוכית ופליז — כל אחד בהקשר האמיתי של ראש דף הבית.
**הכותרת בדף הבית טרם שונתה** · ממתין לבחירה שלה.

## תיקון דף הבית · 3.9.26

**⚠ באג שהשבית את דף הבית לגמרי — שווה לזכור.** בהחלפת בלוק הסיפור
חתכתי עם `s.index('</div></sc-if>')`, וה-`</div>` שנצרך היה **סוגר
החלונית** ולא סוגר הסיפור. התוצאה: ה-`div` של חלונית הסיפור נשאר
פתוח, **כל תוכן העמוד נבלע לתוך ה-`sc-if` המוסתר**, והארטבורד נראה ריק.
`node --check` ובדיקת החורים עברו — רק ספירת תגיות תופסת את זה.
**מעכשיו: אחרי כל חיתוך מארקאפ להריץ בדיקת איזון תגיות
(`html.parser` עם מחסנית) על כל הארטבורדים, לא רק ספירת `<sc-if>`.**

**`Admin.dc.html` · המכסות בדשבורד.** `QUOTA_STEP = 1` במקום 10.
ל-`QUOTAS` נוסף `open`, ומוצגות **רק הקטגוריות של ימי המכירה הפתוחים**
— ביום קוסקוס אין מכסות שישניצל ולהפך. הטבעת הפנימית והחיצונית
נצבעות בגוון של הקטגוריה שלהן, והפנימית מופיעה רק כששני ימים פתוחים
(`hasRing2`).

**התמונות · שקד העלתה `logo.png` ו-`logo-wide.png`.** שניהם היו
~1.5 MB, מעל התקרה של הקנבס. הוקטנו: `logo.png` ל-180px (52 KB,
שקיפות נשמרה) ו-`logo-wide` הומר ל-JPEG 560px (28 KB, אין לו שקיפות).
**המקורות נשמרו ב-`design/app/assets/originals/`** ואינם נאספים ל-seed.
`scripts/seed.sh` תוקן לאסוף כל סיומת בנפרד — ה-brace-glob לא עבד.

**כותרת הבית · סבב שני** באותו ארטיפקט:
`https://claude.ai/code/artifact/6133d4c3-65b9-4e3e-89cf-6aa42014c6c7`
הסבב הראשון נדחה כולו (״לא מושך את העין״). הסבב השני: מלא וחלול ·
חותמת על קשת · מסטהד בפס סגול · צבע לכל שירות · קונדנסד ענק.
**עדיין ממתין לבחירה.**

## מסטהד שחור־זהב · 3.9.26

**נבחר סגנון 03 · המסטהד.** הצבע הסגול נדחה, ובמקומו **הפלטה של
הלוגו עצמו: שחור חמים וזהב.** `.band` הוא פס מלא מקצה לקצה
(`margin: 0 -18px`) ברקע `#1B1714 → #12100E` עם קו זהב דק בתחתית;
השם ב-Anton עם גרדיאנט זהב (`#B98F32 → #F0D68C → #D4AF37`) והשורה
״אוכל ביתי · ארוחות שף · עמדת טאבון״ מתחתיו בזהב עמום. כפתור
הפרופיל הפך לעיגול זהב שקוף על הפס.

⚠ **גובה הפס מוגבל ל-58px** כי הכרטיס שהיא מיקמה ידנית ב-Guest יושב
ב-`top: 64px`. כל הגדלה של הפס תדרוס אותו.

הוחל ב-`Guest.dc.html` וב-`Main.dc.html`. ב-Login הלוגו כבר במרכז,
ולכן שם אין פס.

**ההקלדה בסיפור הואטה.** `TYPE_MS = 20` עם `TYPE_STEP = 1` —
כ-16 שניות ל-807 התווים (קודם 4 שניות). `TICK_EVERY = 4` משמיע
נקישה כל אות רביעית, כי נקישה לכל אות בקצב הזה נשמעת כזמזום.
נוספה שורת **״לחצי לדילוג״** שמופיעה רק בזמן ההקלדה.

**קול ההקלדה הוחלף.** האוסילטור המשולש נשמע כמו ציוץ. עכשיו
`noise()` בונה פעם אחת buffer של רעש לבן, וכל נקישה היא פרץ שלו
דרך `bandpass` ב-1900–2800Hz שנחתך ב-28ms, ומתחתיו סינוס שיורד
מ-150Hz ל-72Hz — הנקישה והחבטה של מכונת כתיבה.

**`scripts/optimize-assets.sh` · חדש.** מקטין אוטומטית כל תמונה
מעל 52KB: שקופה נשארת PNG ומוקטנת ברזולוציה, אטומה מומרת ל-JPEG.
המקור נשמר ב-`assets/originals/`. `seed.sh` מריץ אותו לפני כל בנייה,
כך ששקד יכולה להעלות תמונות בגודל מלא בלי לחשוב על זה.

## קרוסלת התמונות · 4.9.26

**דף הבית סודר מחדש.** הסדר עכשיו: פס הכותרת → **קרוסלת תמונות**
(רווח 14px) → הכרטיס האישי → קרוסלת הקטגוריות → נקודות → כפתור.
המדידה בפועל: פס 30–91, רצועה 105–212, כרטיס 224–385, קטגוריות
387–818 — הכול נכנס ב-844.

**`.reel`** היא מרקיזה: `SHOTS` מוכפל (`SHOTS.concat(SHOTS)`) ו-CSS
מזיז את הרצועה 50% ב-34 שניות בלולאה, כך שהתפר לא נראה. עצירה
ב-hover ו-`prefers-reduced-motion`.
⚠ `width: 100%` ו-`min-width: 0` על `.reel` הם חובה — בלעדיהם
`width: max-content` של הרצועה מרחיב את כל העמוד.

**לחיצה על אריח פותחת תמונה מלאה**: רקע מטושטש (`blur(16px)`),
שם המנה ותת־הכותרת מעל, ואיקס קטן בפינה השמאלית העליונה.

**⚠ הכרטיס האישי הועבר ממיקום מוחלט לזרימה.** הוא היה
`position: absolute; top: 64px` בתוך הכותרת, ולכן חסם כל שינוי מעליו.
עכשיו הוא `align-self: center` עם אותם מידות בדיוק (344×161).
במקביל הוסר `margin-top: 160px` מקרוסלת הקטגוריות — הוא היה הרווח
שהוקצה לכרטיס המוחלט.

**`scripts/render-static.mjs` (בסקרצ׳פד) · כלי חדש שהוכיח את עצמו.**
הוא ממלא את החורים מ-`renderVals()`, פורש `sc-for`/`sc-if` וכותב HTML
סטטי שאפשר לפתוח בדפדפן ולמדוד. **ככה נמצאו המידות האמיתיות במקום
לנחש.** מומלץ מאוד לשחזר אותו בסשן הבא.

**סבב שלישי לכותרת ולאפקט** באותו ארטיפקט:
`https://claude.ai/code/artifact/6133d4c3-65b9-4e3e-89cf-6aa42014c6c7`
חמישה טיפולים לפס (זכוכית כהה · קרם וזהב · פס זהב מלא · מסגרת
שחור־זהב · בלי פס) וחמישה אפקטי הקלדה שרצים חי (קצב אנושי ·
מילה־מילה מטשטוש · דיו נספג · אותיות נוחתות · הקלדה עם תיקון).
**שניהם ממתינים לבחירה.** הפס השחור האטום נדחה.

## הקלדה בקצב אנושי · 4.9.26

**נבחר ב1 · קצב אנושי.** ההשהיה הקבועה הוחלפה בהשהיה לכל תו:
`TYPE_BASE = 11` ועוד `TYPE_JIT = 9` אקראי, `PAUSE` מוסיף 30ms
לרווח, 120 לפסיק ו-200 לנקודה, ו-`HESITATE` (3%) מוסיף 100ms של
היסוס. הלולאה עברה מ-`setInterval` ל-`setTimeout` רקורסיבי
(`step()`), כי הקצב משתנה בכל אות. משך: כ-22 שניות ל-807 תווים.

**⚠ הקול לא נשמע — הסיבה והתיקון.** ה-AudioContext נוצר בתוך
`tick()`, שרץ מטיימר ולא ממחוות משתמש, ולכן הדפדפן השאיר אותו
`suspended`. עכשיו `unlock()` נקרא **בתוך `openAbout()`** — הלחיצה
שפותחת את הסיפור — יוצר את ה-context, את ה-master gain, מריץ
`resume()` ומנגן buffer אילם (חלק מהדפדפנים דורשים ניגון בפועל
בתוך המחווה). בנוסף העוצמות הועלו: רעש 0.5 וחבטה 0.3 דרך master
gain של 0.9 (קודם 0.16/0.09 ישר ל-destination).

**נוסף פעמון סוף שורה.** `LINE_ENDS` מחזיק את המיקומים שבהם שורה
נגמרת, ושם `ding()` מנגן שתי הרמוניות (1568Hz ו-2093Hz) ואחריהן
חריקת חזרת גררה — רעש מסונן שעולה מ-900Hz ל-2600Hz. זה הצליל שבאמת
אומר ״מכונת כתיבה״. הנקישה הרגילה מושמעת כל אות רביעית.

**שלוש כותרות בדף הבית המלא:**
`https://claude.ai/code/artifact/7c84ac29-27d4-4e52-ab36-be4cb9d55838`
א2 קרם וזהב · א4 מסגרת שחור־זהב · א5 בלי פס — כל אחת על דף הבית
האמיתי, מרונדרת מהארטבורד עצמו דרך `scripts/render-static.mjs`.
**ממתין לבחירה.**

## מה פתוח בעיצוב

1. **התמונות** — הלקוחה תעלה. מקומות מסומנים במסגרות מקווקוות:
   5 כרטיסי הבית, 3 מנות קוסקוס, 2 סוגי שישניצל, מארזי ספיישל,
   4 סוגי אירוע בחלה לכל אירוע, 4 מגשי פירות, קרוסלת פינת השף, ומפת החניה.

2. ~~`Admin.dc.html` מארקאפ קשיח~~ **נסגר 1.9.26.** עבר למנוע נתונים,
   וכל שישה אריחי הבנטו וארבעת פריטי הנאב בר מצביעים על ארטבורד אמיתי
   (`TILES[].file`). לכל אריח יש תג מונה. המכסות והטבעת חיים.
   **בקנבס אין ניווט בין ארטבורדים** — המצביעים הם הכנה לפיתוח.

3. **כל נתוני ההדגמה בצד הניהולי נכתבו על ידי Claude** — הזמנות,
   לקוחות, הוצאות, פריטי מלאי ועלויות המנות. המחירים נלקחו מהמסכים
   הקיימים ונכונים; **העלויות והרווחיות אינן.** שולי הרווח שיוצאים
   (52%–70% למנה, 55% שנתי) אופטימיים מדי לעסק אוכל.

4. **מסד הכתובות** ב-`Profile.dc.html` הוא רשימת הדגמה של עשר ערים.
   המקור האמיתי הוא Google Places או data.gov.il ודורש מפתח ושרת.
   להחלפה צריך רק להחליף את `STREETS`. אותו דבר בארוחת שף.

5. **התאריכים החסומים** הם עדיין אלה שהועתקו מהשאלון הישן ולא אושרו.
   פחות קריטי עכשיו — הם נשלטים מ-`AdminDays.dc.html`.
   **שבתות לא נחסמות אוטומטית** · בשאלון הישן כן. לא מומש כלל כזה.

6. **עגלת קניות · ירדה מהשולחן 1.9.26.** הבעיה שהועלתה: קוסקוס הוא
   שלישי, שישניצל שישי, ומגשים כל השבוע — עגלה אחת מניחה תאריך אחד,
   שעה אחת וכתובת אחת. ושף וטאבון הן פניות ולא רכישות.
   ההמלצה הייתה עגלה למוצרים בלבד. **שקד ביקשה לעזוב בינתיים.**

7. **פרטי משלוח למגשים ולמארזים** — נשמרו 20 ₪ ביבנה ו-60 ₪ בשפלה כמו
   בקוסקוס. לא אושר שזה נכון לקטגוריות האלה.

8. ~~מצב ״בוטלה״ חסר~~ **נסגר 1.9.26.** ״בוטלה״ אינו חלק מהמסלול
   הליניארי — אפשר להגיע אליו מכל מצב שטרם נמסר, ולא לצאת ממנו.
   הביטול פותח חלונית עם סיבה (ארבע אפשרויות + הערה חופשית) ומציגה
   מראש אם חלים דמי ביטול: `LATE_HOURS = 12` ו-`LATE_FEE = 0.3`.
   הזמנה מבוטלת יוצאת מהמחזור ומהמונה ״עוד לא נמסרו״, מקבלת טאב
   משלה, ומאבדת את כפתורי הפעולה. בצד הלקוחה היא מוצגת באדום,
   עם שורת דמי הביטול ובלי ״להזמין שוב״.
   **בלוח האייפד אותה חלונית בדיוק** · אותם קבועים, אותן ארבע סיבות
   ואותו חישוב. ההזמנה יוצאת מהלוח (הוא תצוגת בישול), המנות חוזרות
   למלאי, ומונה ״בוטלו N״ מופיע בכותרת.

9. **״נמסרה״ הוא סוף חד־כיווני** בלוח האייפד ובמסך ההזמנות — אין
   ביטול אם סומן בטעות.

10. **היסטוריית רשימות קניות** — יש רשימה פעילה אחת. אין שכפול
    מרשימה קודמת, שזה כנראה מה שיקרה בפועל כל שבוע.

11. **סוג התוצר** — כל המסכים לחיצים ועובדים באמת. לא הוכרע אם ממשיכים כך.

## לקחים שנצברו בסשן העיצוב

**המנוע הגנרי הוא הנכס.** `Boxes.dc.html` ו-`Chef.dc.html` חולקים אותו מנוע
מקטעים. להוסיף שדה = להוסיף `kind` אחד, לא לשכתב מסך.

**הרצת סימולציה במקום בדיקת דפדפן.** אי אפשר לבדוק בדפדפן בסשן הזה. במקום זה:
לחלץ את הסקריפט, לעטוף ב-`class DCLogic { setState(o){ Object.assign(this.state,o); } }`
ולהריץ `renderVals()` ב-node. זה תפס באגי תמחור, נעילה והצגה מותנית לפני פרסום.

**אימות מבני לפני כל פרסום:** ספירת `<div>/<sc-if>/<sc-for>/<svg>`, `node --check`
על הסקריפט, ובדיקה שכל `{{ hole }}` מקבל ערך ב-`renderVals`.

**החלפת מחרוזות עם `assert count == 1`.** כל עריכה מאמתת שהמחרוזת קיימת
פעם אחת בדיוק. זה תפס עשרות טעויות.

**הנפשות בגיליון ולא בסגנון האלמנט** — פאנל התכונות של הקנבס יכול לדרוס
סגנון על אלמנט, ולכן הקרוסלה בפינת השף מונפשת ב-CSS בגיליון העליון.

### לקחים שנוספו 1.9.26

**לא לנחש באג ויזואלי · לרנדר ולמדוד.** שקד דיווחה ש״האליפסה לא
סימטרית״ בעמוד ההתחברות. ניתוח בקוד הצביע על הכדור — הוא היה תקין
לגמרי (84×84). בניתי שכפול סטטי, פתחתי בדפדפן ומדדתי, והתברר שהבעיה
היא **הידית במתג**: רווח 4px למעלה ו-12px למטה, כי לקונטיינר עם
`height` ו-`padding` **חסר `box-sizing: border-box`** והריפוד נוסף
לגובה. בלי המדידה הייתי מתקן את הדבר הלא נכון.

**`box-sizing: border-box` לא יורש.** הוא מוגדר על ה-div החיצוני של
המסך ולא עובר לילדים. כל אלמנט עם `height` **וגם** `padding` חייב
אותו במפורש. זה הבאג שנתפס למעלה.

**בדיקת ״חורים ריקים״ ברג׳קס נותנת false negative.** החיפוש של
`\bkey\s*:` מוצא גם `setState({ saved: true })` וחושב שהמפתח קיים
ב-`renderVals`. הדרך הנכונה: לחתוך מ-`return {` והלאה ולחפש רק
`^      key:` עם עוגן שורה. כך נתפס `saved` שנמחק בטעות.

**חיתוך בלוק ערכים לפי אינדקסים מסוכן.** מחיקת קטע מ-`addrLabel`
עד `securityLabel` בלעה גם את ערכי השמירה וההזדהות שישבו באמצע.
עדיף החלפות מחרוזת ממוקדות מאשר חיתוך טווח.

**`cd` ב-Bash נשמר בין קריאות.** `cd design/app` בקריאה אחת שבר
את ה-diff מול הקנבס בקריאה הבאה, כי הנתיבים היחסיים הפכו לשגויים
וכל אחד עשר הארטבורדים דווחו כ-DIFF. **תמיד נתיבים מוחלטים בבדיקות.**

**`str.replace('', x)` בפייתון מכניס את `x` בין כל שני תווים.**
חיתוך `s[s.index('<script data-dc-script'):s.index('</script>')]` החזיר
מחרוזת ריקה, כי `index('</script>')` מצא את הסגירה של
`<script src="./support.js">` ב-`<head>` — שנמצאת **לפני** תג הפתיחה.
התוצאה: קובץ של 124MB במקום 30KB. **תמיד לחפש את הסוגר החל מהפותח**
(`s.index('</script>', js0)`), ותמיד לאמת את גודל הפלט לפני כתיבה.

**חיתוך טווח לפי index חייב גבול עליון ואסרשן.** חיתוך שורות המכסה
בדשבורד עד `'</div>\n\n</div>'` תפס סוגר רחוק ובלע את כפתור ההזמנה
הידנית ואת מדדי היום. הגבול הנכון היה ההערה שמופיעה מיד אחרי הבלוק.
מאז כל חיתוך טווח בקוד הזה נבדק ב-`assert 0 < a1-a0 < N`.

**נרמול טלפונים מיושם בשלושה מקומות** — הזמנה ידנית, חיפוש לקוחות,
והתאמת לקוחה קיימת. `norm()` מסיר מקפים ורווחים וממיר `+972` ל-`0`.
נבדק ש-`0501234567` מוצא רשומה ששמורה כ-`050-1234567`.
