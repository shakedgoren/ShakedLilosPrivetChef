# BITE & TELL · מסמך המשכיות

> עודכן: 14 בספטמבר 2026, **סבב ב׳** (סוף היום). הקובץ מתאר את המצב **הנוכחי** של הפרויקט.
> אם קראת אותו — יש לך את כל מה שצריך כדי להמשיך בלי שאלות פתיחה.

**העסק:** שקד לילוז · שף פרטית וקייטרינג · מותג **BITE & TELL**.
**Git:** `https://github.com/shakedgoren/ShakedLilosPrivetChef.git` · ענף `main`.
**תיקיית עבודה:** `/Users/shakedgoren/Downloads/files`

---

## כללי הברזל — לקרוא לפני כל פעולה

### 1 · לפני כל פרסום לקנבס

> כלל ברזל אחד: אני עורכת את העיצוב בקנבס ושומרת. לפני שאתה מפרסם משהו —
> תקרא את הארטיפקט עם `Artifact action:"read"`, תחלץ עם `seed-canvas.mjs --extract`
> לתיקייה ריקה, תשווה לקבצים המקומיים, ותאמץ את הגרסה שלי כבסיס. ואז תאמת
> ב-grep ובגודל הקובץ שהעריכות שלי שרדו את המיזוג. פרסום בלי זה דורס לי את
> העבודה, וזה כבר קרה פעם אחת.

### 2 · שלושה כללים קבועים

> 1. אסור לשנות טקסטים שאני כתבתי. אם אתה מוסיף טקסט חדש, תגיד לי במפורש שאתה כתבת אותו.
> 2. אל תוסיף אלמנטים או סעיפים שלא ביקשתי. תציע במקום.
> 3. בסוף כל משימה: `git add -A && git commit && git push origin main`

### 3 · רוב קבצי `mobile/src/data/*.ts` נוצרים אוטומטית — אסור לערוך אותם ביד

כל שינוי בהם נמחק בהרצה הבאה של ה-emitter. **זה כבר קרה פעמיים:**
הרצה חוזרת של `emit-admin-orders.mjs` מחקה תוספות שקורסור כתב, ו-`adminMenu.ts`
נכתב פעם אחת ביד מול emitter ישן — עד שהרצת ה-emitter שברה את המסך.
תוספות שצד השרת צריך נכנסות ל-`scripts/emit-*.mjs` עצמו, ושם הן מסומנות `⚠ לא מהקנבס`.

**איך יודעים אם לקובץ יש emitter:** `grep -l "data/<שם>.ts" scripts/*.mjs`.

**חמישה קבצים נכתבים ביד ומותר לערוך אותם:**
`boxes.ts` · `schnitzel.ts` · `couscous.ts` · `categories.ts` · `shared.ts`.
גם בהם, כל דבר שאינו מהקנבס מסומן `⚠ לא מהקנבס` עם הסבר.

---

## שלוש השכבות של הפרויקט

| שכבה | מה זה | מצב |
|---|---|---|
| `chef-questionnaire.html` | הגרסה הראשונה · HTML יחיד | **ארכיון.** מקור אמת לטקסטים ומחירים בלבד |
| `design/app/` | קנבס העיצוב · 22 מסכים | **פעיל.** שקד עורכת שם ושומרת |
| `mobile/` | האפליקציה האמיתית · React Native | **22/22 המסכים הועברו.** אפס בדיקות |
| `server/` | Express + Prisma · קורסור בנה | **רץ מקומית.** 8 טבלאות, 17 בדיקות עוברות |

---

## איפה עומדים עכשיו

**מה עובד end-to-end מקומית:**
כל 22 המסכים · 11 מסכי הניהול מושכים נתונים אמיתיים מהשרת · DB מקומי זרוע ·
התחברות, הזמנות וניהול. הרשמה נבדקה מקצה לקצה (טוקן חוזר מהשרת).

**סבב העיצוב מול הקנבס הושלם** (10 קומיטים, 9–11 בספטמבר). כל חמשת מסכי
הלקוחה עברו התאמה אחד לאחד למרקאפ. הפרטים בסעיף ״שפת העיצוב באפליקציה״.

**סבב הליטוש מול שקד — 14 בספטמבר** (5 קומיטים, `b2b45af`…`4416dac`).
כולו תיקונים שהיא ביקשה בצפייה חיה. הפירוט בסעיף ״סבב 14 בספטמבר״.

**מה שנשאר, לפי סדר:**

1. ~~לאמת את כניסת המנהלת~~ — **נסגר בסבב ב׳.** אומת בדפדפן בשלושה מסלולים ועוד בדיקת נסיגה של לקוחה רגילה.
2. ~~שמות התמונות ברצועה~~ — **נסגר.** `photoTitles.ts` מכיל את 36 השמות של שקד. הקובץ **מתוחזק ביד** ואין לו מחלץ. **אין להמציא בו שמות.**
3. **פריסה** — Postgres + hosting. **החלטה של שקד, יש עלות חודשית.** ההמלצה: Railway Hobby ~$5–10 לחודש (בערך 20–40 ₪). לא להתחיל בלי אישור מפורש.
4. **סליקה · וואטסאפ** — תהליכי אישור של שבועות. על שקד להתחיל אותם.
5. **3 התמונות החסרות במסך השף** — שקד הניחה את שלושתן ב-`design/app/assets/originals/`: `hagasha-ishit.png`, `kinuhim-table.png`, `drink-no-limit.png`. **עדיין לא עברו `sync-photos.sh` ולא מופו ל-`photos.ts`.** לשאול אותה איזה שם שייך לאיזה סעיף לפני מיפוי.
6. ~~תמונות לכרטיסי `cards` בספיישל~~ — **נסגר.** ארבע תמונות האירוע בחלה ממופות ב-`mobile/src/screens/boxes/eventPhotos.ts`. ⚠ הקובץ הזה **אינו מהקנבס** — המיפוי התקבל משקד בהודעה.
7. **בדיקות באפליקציה** — **אפס.** `npx jest` מוצא 0 קבצים מתוך 138. המסמך טען פעם שיש שלוש בדיקות ל-`whatsappOrder` — אין. בשרת יש 17.
8. ~~כפתור החזרה בספיישל~~ — **נסגר בסבב ב׳.** תו הטקסט ״›״ היה מתהפך בשיקוף הדו-כיווני ולכן נראה הפוך; המעבר ל-`BackButton` המשותף יישר אותו לכל המסכים.

### מה לא אומת — להגיד לשקד

- **הכל נבדק בדפדפן בלבד.** שום דבר מכל הסבבים לא נבדק על אייפון.
- **אפס בדיקות אוטומטיות באפליקציה.** כל האימות ידני-מדיד. בשרת יש 17 בדיקות.

### שלוש השאלות שהיו פתוחות — נסגרו ב-11 בספטמבר

1. **מסגרת עומדת מול ריבועית** — **שקד בחרה ריבוע.** קבצי המקור הם 480×480,
   ומסגרת עומדת הייתה חותכת מהצדדים. הריבוע מראה את כל התמונה בפרופורציה.
   סגור — לא לפתוח מחדש.
2. **שלושת הנוסחים שכתב Claude — כולם אושרו:** הודעת הוואטסאפ למיכל,
   ״להזמנה בוואטסאפ״, ״בחירת צורה״ בשניצל. מעכשיו הם טקסטים של שקד וחל עליהם
   כלל הברזל ״אסור לשנות טקסטים שאני כתבתי״.
3. **ההילה של כפתור ״להתחברות והזמנה״** — **שקד ביקשה עוד זוהר.** ההילה הוגברה:
   `CTA_GLOW_SPREAD` 18→30, `CTA_GLOW_BLUR` 12→18, והשקיפויות עברו לקבועים
   `CTA_GLOW_CORE_ALPHA` 0.68 ו-`CTA_GLOW_MID_ALPHA` 0.3 ב-`theme/glass.ts`.
   אומת בדפדפן: ההילה 310×100 מול כפתור 250×40, `blur(18px)`, והלחיצה עדיין
   מגיעה לכפתור ומעבירה לטאב הכניסה. **אינה מהקנבס** — בקנבס יש רק צל עדין.

---

## סבב 14 בספטמבר · מה השתנה ולמה

כל הסעיפים כאן הם בקשות של שקד מצפייה חיה. **כולם אומתו במדידה בדפדפן**
פרט לכניסת המנהלת.

### הניווט — ההתחברות היא שכבה, לא מסך

`navigation/store.tsx` · **השינוי המשמעותי ביותר בסבב.**
חסם ההתחברות מבטיח ״הבחירות שלך נשמרות · אחרי ההתחברות חוזרים בדיוק לכאן״,
והוא לא עמד בזה: `signIn` תמיד העביר ל-`main`. תיקון ראשון החזיר למסך הנכון —
**וגם זה לא הספיק**, כי המעבר למסך ההתחברות מפרק את מסך ההזמנה ומאפס את הסל.
נמדד: חזרנו לקוסקוס עם 0 מנות.

**הפתרון:** `goLogin()` מרים `loginOverlay`, ו-`App.tsx` מרנדר את `LoginScreen`
בתוך `Modal` מעל המסך הנוכחי. המסך שמאחור נשאר מרונדר וכל ה-state שלו חי.
`signIn` סוגר את השכבה ולא נוגע ב-`screen`.

> **אומת מקצה לקצה:** מנה בקוסקוס → המשך → החסם → התחברות → חזרה לקוסקוס
> עם ״מנה אחת · סה״כ 45 ₪״ שמורים.

⚠ **אסור להחזיר את ההתחברות למסלול רגיל.** כל זרימות ההזמנה יאבדו את המצב.
⚠ ב-`LoginScreen`, ״להסתכל בלי חשבון״ סוגר את השכבה במצב שכבה, ולא הולך ל-`guest`.

**כניסת מנהלת** מנתבת ל-`admin` ולא ל-`main` — גם ב-`signIn` וגם בשחזור הטוקן.
החלטה 12 אומרת שהגישה לניהול היא רק דרך ההתחברות של שקד. **לא נבדק בדפדפן.**

### `pointerEvents` — שתי מלכודות שכבר עלו ביוקר

**1 · חלון הקרוסלה בלע את שורת המכירה.** ל-`CategoryCarousel` יש ריפוד
`GLOW_ROOM` ומרג׳ין שלילי, ולכן הוא מכסה את שורת המכירה שמעליו. שקד דיווחה
ש״רק הצד הימני לחיץ״; המדידה הראתה ש**אף נקודה לא הגיעה לשורה**. הפתרון:
`pointerEvents="box-none"` על החלון. המחוות יושבות על המסלול הפנימי, אז החלון
עצמו לא צריך מגע.

**2 · `GlassFill` כבר מסומן `pointerEvents="none"`.** אל תסיר.

### הנאב-בר חסם את כפתור ההמשך

`components/BottomNav.tsx` מייצא שני קבועים שנגזרים מ-`Order.dc.html`:
הנאב יושב ב-`bottom: 26` בגובה 68, ושורת הסה״כ ב-`bottom: 102` — שמונה מעליו.

- `BAR_BOTTOM_WITH_NAV` — לשורה תחתונה בזרימה. מוחל ב-`cous`, `schn`, `box`,
  `fruit`, `chef` דרך `barWithNav`, מותנה ב-`loggedIn`.
- `SCROLL_PAD_NAV` — ריפוד תחתון לאזור גלילה שרץ עד התחתית. מוחל ב-`chef`
  (ענף המבוא), `box`, `orders`, `profile`.

**נמדד אחרי התיקון:** 20–22 פיקסלים בין הכפתור לנאב בכל המסכים, וכל הכפתורים
נגישים ל-`elementFromPoint`.

### שטח מת בגלילה

שקד ביקשה שלא יהיה לאן לגלול כשאין תוכן. בדף הבית אחרי התחברות היו
**160 פיקסלים** של ריק (`paddingBottom: 120` ועוד `marginBottom: 40` של הרצועה).
`HOME_PAD_NAV = BAR_BOTTOM_WITH_NAV − REEL_BOTTOM` פותר את זה.
**נמדד:** הרצועה נגמרת ב-716, הנאב מתחיל ב-718. לפני התחברות `canScroll: 0`.

### ״הנקודה״ על בועות הקטגוריות — שלוש שכבות, לא אחת

זה לקח שלושה סבבים כי הסרתי פעמיים את השכבה הלא נכונה. לבועה יש **שלושה**
מקורות אור, וכולם מהקנבס:

| שכבה | איפה | האם זו ״הנקודה״ |
|---|---|---|
| אליפסה לבנה מעל הבועה | `Guest.dc.html:181` | לא |
| גרדיאנט ברק על הגוף | `Guest.dc.html:681` | לא |
| **עוצר `#FFFFFF` במרכז גרדיאנט הגוף** | `Guest.dc.html:681` | **כן** |

`Orb.tsx` · `spark={false}` מכבה את שלושתן. `CategoryRail` מעביר אותו;
מדליית הכרטיס נשארה עם האור. **הבועות באפליקציה כבר לא זהות לקנבס.**

> **השיטה שעבדה:** לבודד שכבה אחת בכל פעם ב-DOM החי ולצלם. ניחוש מהקוד נכשל פעמיים.

### שאר הליטוש

- **חמש הנקודות בכרטיס** — המתכון מ-`Guest.dc.html:566–569`: גובה 6, מרווח 6,
  הפעילה ברוחב 20 ובגוון הקטגוריה. מחושבות פעם אחת ב-`CategoryCarousel`
  ומועברות לכל הכרטיסים, בדיוק כמו בקנבס.
- **שורת המכירה** — `NEXT_SALE` ב-`HomeScreen`. ⚠ הערכים מוטבעים מהקנבס ו**לא
  מגיעים מהשרת.** כשיהיה מקור אמת ליום המכירה — להחליף שם.
- **זוהר** — `CTA_GLOW_*` (כפתור ההתחברות) ו-`CARD_GLOW_*` (הכרטיס הראשי) הוגברו
  לבקשת שקד פעמיים. **אינם מהקנבס.**
- **מרווחי דף הבית** — `TIGHTEN_TOP`/`TIGHTEN_BOTTOM` ב-`CategoryCarousel`,
  `RAIL_GAP` ב-`HomeScreen`. כולם לא מהקנבס.
- **מסך השניצל** — מחירים במשקל 700, כותרת ״ההזמנה שלי״ מעל הפריטים בשני
  הטאבים (רק כשיש), והמארזים עלו מעל ״בחירת צורה״.
- **מסך הפירות** — שורות הפעילות עברו אל תוך `CategoryHeader` (שמקבל עכשיו גם
  מערך), והריפוד 88→105. הכותרת והשורה הראשונה זהות לקוסקוס לפיקסל;
  התיאור 17 פיקסלים נמוך יותר כי יש שתי שורות במקום אחת — **גאומטרית אי אפשר
  לקבל את שניהם.**
- **`EyeToggle`** — מתג הצגת סיסמה. ⚠ נכתב כרכיב נפרד ו**לא** ב-`icons/index.tsx`,
  שנוצר אוטומטית מהקנבס.
- **ה-Lightbox** — לחיצה על התמונה סוגרת (בנוסף לאיקס), הרצועה נעצרת כשתמונה
  פתוחה, ושם התמונה יוצג מעל במרכז כשתגיע הרשימה.

### `api/client.ts` — timeout

`fetch` אל שרת שלא מגיב **נתקע עד שמערכת ההפעלה מוותרת** — נמדד מעל 6 שניות
בלי שגיאה. לכן מסך ההתחברות לא הציג שום הודעה כששקד ניסתה להתחבר וה-IP היה ישן.
`AbortController` עם 10 שניות. **אומת:** השרת הופל בכוונה, ההודעה
״לא ניתן להתחבר כרגע״ הופיעה תוך 253ms.

⚠ **`mobile/.env` ב-gitignore.** ה-IP השתנה פעם אחת באמצע הסבב
(`192.168.1.192` → `192.168.1.164`) ושבר את כל האפליקציה בלי שום הודעה.
**כשמשהו לא עובד — לבדוק את זה ראשון:** `ipconfig getifaddr en0`.
אחרי עדכון הקובץ **חייבים להפעיל מחדש את שרת ה-Expo** — המשתנה נקרא בזמן בנייה.

---

## שכבה 2 · קנבס העיצוב

22 ארטבורדים ב-`design/app/*.dc.html` + `canvas.json` + 80 תמונות ב-`assets/`.

**הארטיפקט:** `https://claude.ai/code/artifact/1f045445-e886-46ce-b3b3-8082b95742a3`
**כותרת:** `BITE and TELL` (הסידר דוחה `&` בכותרת · המסכים משתמשים ב-`BITE &amp; TELL`)

### בנייה ופרסום

```bash
export SK=<נתיב הסקיל design>        # משתנה בין סשנים · מריצים /design כדי לקבלו
export SP=<תיקיית סקרצ׳פד>            # ה-extractors כותבים לשם JSON ביניים
bash scripts/seed.sh                  # optimize-assets ואז סריקת 22 המסכים וכל התמונות
```
ואז `Artifact` עם `url` של הארטיפקט, `contract: "0.1.31"`, `favicon: "👨🏻‍🍳"`, **בלי** `capabilities`.

### שרשרת האימות לפני כל פרסום

1. החלפת מחרוזת מדויקת עם `assert count == 1`
2. בדיקת איזון תגיות (`html.parser` עם מחסנית) על כל 22 המסכים
3. כל `{{ חור }}` מקבל ערך מ-`renderVals`
4. `node --check` על בלוק ה-JS של כל מסך
5. סימולציה ב-node של זרימות אמיתיות
6. grep + גודל קובץ על המטען הסרוק

**למה זה קיים:** פעם אחת חיתוך מרקאפ בלע את כל תוכן דף הבית לתוך `sc-if`
מוסתר והדף יצא ריק. `node --check` ובודק החורים עברו בהצלחה. רק בדיקת
איזון התגיות תופסת את זה.

> בודק החורים מדווח false positives על מפתחות דינמיים (`vals['q' + i]`),
> קיצור אובייקט, ושני מפתחות באותה שורה. צריך לנכות משתני `sc-for as=""`.

### 22 המסכים

**לקוחה:** `App` (מעטפת ניווט) · `Guest` · `Main` · `Login` · `Order` (קוסקוס) ·
`Schnitzel` · `Boxes` (ספיישל) · `Fruit` · `Chef` · `MyOrders` · `Profile`
**ניהול:** `Admin` · `AdminOrders` · `AdminBoard` (אייפד 1180×820) · `AdminDays` ·
`AdminMoney` · `AdminShopping` · `AdminHistory` · `AdminStock` · `AdminCustomers` ·
`AdminMenu` · `AdminCosts`

### שפת העיצוב

- גופן Assistant · רקע `#FCFBFB` · טקסט `#2A2430`
- זהב המותג: `#8A6A1F → #D4AF37 → #C9A227` · טקסט זהב `#A9812A` · רך `#9C7F3F`
- חמישה גווני קטגוריה: קוסקוס `#7B5CBC` · שניצל `#416D9E` · ספיישל `#437C59` ·
  פירות `#B04A76` · שף `#A85A28`
- RTL · גרשיים עבריים `׳`/`״` במחרוזות JS · **אין מע״מ** (עוסק פטור)

### פגמים ידועים בקנבס — לא תוקנו

- **`Main.dc.html` · כפתורי הנאב מחווטים הפוך.** אייקון הבית קורא ל-`navProfile`
  ואייקון המשתמש קורא ל-`navHome`. באפליקציה זה נכון. שקד לא החליטה אם לתקן.
- **`AdminBoard` · שורת הסיכומים מוסטת ב-114px.** שורות ההזמנות משתמשות ב-92+220
  ו-68 לעמודה, שורת הסיכום ב-282 ו-92 — הסטייה מצטברת. **יושר בהעברה, שקד אישרה
  להשאיר מיושר בינתיים.**
- **`AdminMenu` · העלויות עדיין מוקלדות.** ארטבורדים לא מייבאים זה מזה, אז
  החישוב האוטומטי חי ב-extractor. הערכים בקנבס יושרו ידנית; ה-extractor
  נכשל אם נוצר פער.

---

## שכבה 3 · האפליקציה

`mobile/` · Expo SDK 57 + React Native 0.86 + TypeScript · קוד אחד לאייפון ולאנדרואיד.
`AGENTS.md` דורש לקרוא את התיעוד הממוספר של אקספו — `https://docs.expo.dev/versions/v57.0.0/` — לפני כתיבת קוד.

```bash
cd mobile && npx tsc --noEmit                    # בדיקת טיפוסים
npx expo export --platform ios --output-dir /tmp/x   # בדיקת bundle
```

**`mobile/.env`** (ב-gitignore, לא ב-git):
```
EXPO_PUBLIC_API_URL=http://192.168.1.192:3001
```
זו כתובת הרשת של המחשב — עובדת גם בדפדפן וגם באייפון באותה Wi-Fi.
**אם ה-IP משתנה צריך לעדכן.** ריק = מצב דמה.

**תצוגה חיה:** `.claude/launch.json` מגדיר `bite-and-tell` שמריץ `expo start --web`
על פורט 8081. משתמשים ב-`preview_start` ואז בודקים עם `javascript_tool`.
לבדיקה בדפדפן יש קישור עומק `?screen=<שם>` — **Claude כתב אותו**, לא קיים במכשיר.
**על האייפון:** `npx expo start` בטרמינל, סורקים את ה-QR עם Expo Go.

### מבנה

```
src/theme/tokens.ts        אסימוני העיצוב · הועתקו אחד לאחד מהקנבס
src/theme/rtl.ts           IS_RTL ו-enableRTL · המקור היחיד לכיוון
src/theme/fonts.ts         שש משקולות Assistant + Anton · כל משקולת משפחה נפרדת
src/theme/applyFonts.tsx   מזריק את המשפחה לפי fontWeight לתוך כל <Text>
src/icons/index.tsx        ⚠ נוצר אוטומטית · 51 אייקונים מהקנבס
src/text/counts.ts         יחיד ורבים בעברית
src/data/                  נתונים שנוצרו אוטומטית · אסור לערוך ביד
src/api/                   הלקוח מול השרת · client, auth, orders, admin, status, storage
src/navigation/store.tsx   מעטפת הניווט · מקבילה ל-App.dc.html
src/order/                 זרימת המסירה המשותפת לכל הקטגוריות
src/theme/glass.ts         מתכוני הזכוכית והזוהר · הועתקו מהקנבס
src/components/            רכיבים משותפים · Masthead, PhotoReel, PhotoStrip,
                           CategoryCarousel, CategoryCard, CategoryRail, Photo,
                           Stepper, BottomNav, PageWash, Orb, Glass, Lightbox,
                           PrimaryButton, CategoryIcon, useCategorySwipe
src/screens/               מסכי הלקוחה · 11
src/admin/                 מסכי הניהול · 11 + ערכת ui/ משותפת
```

### שישה תיקונים שאסור לשבור שוב

**1 · RTL.** `I18nManager.forceRTL(true)` **לא מספיק ב-React Native Web** — הוא לא
נוגע בכיוון המסמך, ו-`I18nManager.isRTL` נשאר `false`. כל 22 המסכים יצאו LTR.
`src/theme/rtl.ts` מסמן `<html dir="rtl">` ומייצא `IS_RTL` כמקור אמת.
**אסור להסתמש ב-`I18nManager.isRTL` בשום מקום.**

**2 · גלילה אופקית ב-RTL.** `contentOffset.x` יוצא **שלילי** (נמדד: 0 עד ‎-1419).
כל חישוב אינדקס ממנו נחתך ל-0. קרוסלת דף הבית עברה למסלול שזז ב-`transform`,
כמו בקנבס — `CategoryCarousel.tsx`, עם `SWIPE_MIN 45` ו-`DRAG_SLOP 6` מהקנבס.
מאותה סיבה `PhotoReel` גוזרת את כיוון האנימציה מ-`IS_RTL`.

**3 · הגופנים.** ב-React Native כל משקולת היא **משפחה נפרדת**; `fontWeight` לבדו
לא בוחר קובץ. `applyFonts.tsx` עוטף את `Text` ומזריק `fontFamily` **בכניסה** ל-render.
ניסיון לשכפל את האלמנט המוחזר עם מערך סגנונות נכשל בשקט על צמתי DOM.

**4 · כיוון הכתיבה של מספרים.** `react-native-web` גוזר את כיוון הטקסט **מהתוכן**.
מחרוזת שמתחילה בספרה — ״45 ₪״, ״052-2958511״, כל מחיר וכל מונה — מסומנת `ltr`,
ו-`text-align: start` מיישר אותה **שמאלה** בזמן ששאר הטקסט מיושר ימינה.
`applyFonts.tsx` כופה `writingDirection` על כל `Text`. **אל תסיר את זה** — כל
מספר באפליקציה יקפוץ לצד הלא נכון.

**5 · `Photo` עוטפת ב-`Pressable`.** בשביל ההגדלה בלחיצה. **עטיפה בלי מידות
מתכווצת לאפס** — כל קורא חייב לתת לתמונה רוחב או גובה. זה כבר הפיל את קרוסלת
השף פעם אחת. הניסיון ״להעביר את אותו סגנון לשניהם״ פתר את הקריסה אבל מתח
את התמונה ל-800 פיקסלים וחתך אותה — **זו לא הדרך**.

**6 · החלקה אופקית באצבע.** `touch-action` חייב להיות `pan-y` על אזור ההחלקה.
בלעדיו הדפדפן במכשיר מגע חוטף את המחווה ושולח `touchcancel`, והקוד לא מקבל
כלום. `SWIPE_SURFACE` ב-`useCategorySwipe.ts` הוא המקום היחיד שמחזיק את זה.
בנוסף, `Pressable` בתוך אזור ההחלקה תופס את ה-responder כבר בנגיעה, ולכן
חייבים `onMoveShouldSetPanResponderCapture` — בלעדיו ההורה לא נשאל שוב.

### שפת העיצוב באפליקציה — הועברה מהקנבס בספטמבר

**הרקע אינו `#FCFBFB` שטוח.** בקנבס יש מעליו שלוש כתמי צבע רכים, ובמסכי
הקטגוריות גם רביעי בגוון הקטגוריה. **בלי השטיפה הזו כל הזכוכית יוצאת
לבן על לבן** — זו הייתה הסיבה שהאפליקציה נראתה שטוחה. `components/PageWash.tsx`,
מותקן פעם אחת ב-`App.tsx`. **מסכים חייבים רקע שקוף** — `page` בלי `backgroundColor`.

**`theme/glass.ts`** מחזיק את כל מתכוני הזכוכית מהקנבס: הגרדיאנטים, המסגרות
הלבנות וערימות הצללים (`boxShadow` ב-RN 0.76+ מקבל מחרוזת CSS מלאה עם `inset`).
`TILE_EDGE` ו-`TILE_SHADOW` הם האריח הסטנדרטי — **מסגרת לבנה, לא אפורה.**

**`components/Orb.tsx`** — הבועה הצבעונית: הילה, גוף, נקודת אור ואייקון.
**`components/Glass.tsx`** — לוח זכוכית. **`components/Lightbox.tsx`** — הגדלת
תמונה. הוא מותקן ב-`App.tsx` ו-`Photo` קוראת לו לבד, ולכן **כל** תמונה
באפליקציה נפתחת בלחיצה. `zoom={false}` מכבה, וצריך אותו בכל כרטיס שכולו כפתור.

⚠ **גרדיאנטים מצוירים ב-`react-native-svg`** ולא ב-CSS. לכל `<Svg>` חייבים
`width="100%" height="100%"` **כ-props** — בווב ברירת המחדל היא 300×150, ובלעדיהם
`Rect` ב-`100%` גולש הרבה מעבר לרכיב.

⚠ **הטשטוש מאחורי תמונה מוגדלת עובד רק בווב** (`backdrop-filter`). במכשיר
נשאר מסך כהה בלבד. טשטוש אמיתי שם ידרוש `expo-blur`.

**הכותרת של מסך פנימי מרחפת** ב-`top: 30`, ואזור הגלילה מתחיל ב-88 — כך בקנבס.
ברשימת הספיישל הכותרת שורה אחת, ולכן שם הריפוד 70.

**`components/Stepper.tsx`** משותף לכל האפליקציה. `tone` לצבעי הכפתורים,
`wide` לפריסה לקצוות, `center` לתוכן שמחליף את המספר.
**הפלוס תמיד מימין והמינוס תמיד משמאל — בקשה מפורשת של שקד.**

### חילוץ נתונים מהקנבס — אל תקליד ידנית

כ-22 זוגות סקריפטים ב-`scripts/`:

```bash
export SP=<סקרצ׳פד>
node scripts/extract-<x>.mjs && node scripts/emit-<x>.mjs   # → mobile/src/data/<x>.ts
```
ה-extractor מושך מהמרקאפ עם `assert found exactly once` — כל כישלון כזה תפס עד היום
בעיה אמיתית. **לעולם לא להקליד את הנתונים ביד.**

**סקריפטים שאינם מהקנבס** (נכתבו על ידי Claude, מסומנים בקובץ עצמו):
- `scripts/lib/dish-costs.mjs` — נמל של `unitCost` מ-`AdminCosts`. מזין את עלויות
  `AdminMenu` במקום שיוקלדו. 17 מתוך 20 הפריטים תאמו מיד — זה מה שאימת את הנמל.
- `scripts/extract-icons.mjs` + `emit-icons.mjs` — ערכת האייקונים. השמות בטבלה
  ב-emitter נכתבו על ידי Claude, ממופים לפי חתימת הצורה כדי שלא יזוזו.
  ⚠ המחלץ **מפצל `<sc-if>` בתוך `<svg>`** לאייקונים נפרדים. בלי זה חמשת
  אייקוני הקטגוריות התמזגו לאייקון אחד עם 14 צורות, ולכן לא היו אייקונים
  בשורת הקטגוריות בדף הבית. 51 אייקונים מכסים 348 מתוך 351 מופעים.

---

## שכבה 4 · השרת

`server/` · Express + Prisma + JWT. **רץ מקומית, לא באוויר.**

```bash
cd server
cp .env.example .env          # פעם אחת
npm install
npx prisma migrate deploy     # יוצר server/prisma/dev.db
npm run db:seed               # חשבון מנהלת + נתוני פתיחה
npm run dev                   # http://localhost:3001 · מאזין ל-0.0.0.0
npm test                      # 17 בדיקות
```

⚠ `npx tsc --noEmit` בשרת מרעיש על `prisma/seed.ts` (הרצה דרך `tsx`).
**האימות בשרת הוא `npm test`, לא `tsc`.**

**התחברות:** השדה נקרא **`who`** (טלפון או אימייל), **לא `email`**.

| תפקיד | טלפון | סיסמה |
|---|---|---|
| מנהלת (שקד לילוז) | `0500000000` | `changeme` |
| לקוחה (דנה כהן) | `0501234567` | `changeme` |

יש עוד שמונה לקוחות זרועות, כולן באותה סיסמה. **לא צריך ליצור משתמשים חדשים.**

**8 טבלאות:** `User` · `PasswordReset` · `Order` · `SaleDay` · `SupplyItem` ·
`ShoppingList` · `ProductionDish` · `Expense`

**DB:** SQLite ב-`prisma/dev.db`. קורסור הוסיף `docker-compose.yml` ו-`scripts/db-mode.mjs`
למעבר ל-Postgres מקומי. לפרודקשן — `DATABASE_URL` ל-Postgres.

### נקודות הקצה שהאפליקציה באמת קוראת

| מסך | נקודת קצה |
|---|---|
| Admin | `/admin/summary` |
| AdminOrders | `/admin/orders` · `/admin/customers` |
| AdminBoard | `/admin/board?date=&category=` |
| AdminDays | `/admin/days?from=&to=` |
| AdminMoney | `/admin/money?period=` |
| AdminShopping | `/admin/shop/active` |
| AdminHistory | `/admin/shop/history` |
| AdminStock | `/admin/stock/sale` · `/admin/stock/supply` |
| AdminCustomers | `/admin/customers` |
| AdminMenu | `/admin/menu` |
| AdminCosts | `/admin/costs` |
| לקוחה | `/auth/*` · `/users/me` · `/users/me/photo` · `/orders` · `/orders/:id/reorder` |

> **⚠ אין `/admin/history` ואין `/admin/days/active`.** היסטוריית הקניות וסגירת
> הרשימה יושבות תחת `/admin/shop`. גרוע מזה: `GET /admin/days/active` **לא מחזיר 404**
> אלא מותאם ל-`/:date` עם `date="active"` ומחזיר `{"date":"active","rec":{}}` —
> תשובה שנראית תקינה אבל ריקה. **לא תוקן.** שווה אימות פורמט תאריך.

---

## התמונות

80 קבצים ב-`design/app/assets/`. **כולן `.jpg` פרט ל-`logo.png`.**
`sync-photos.sh` בוחר לכל שם את הגרסה עם יותר פיקסלים, ואז מקטין. 31MB → 8.6MB.
`check-photos.mjs` גוזר את הקבוצות מ-`photos.ts` עצמו.

- **`home-1..10`** — רצועת התמונות הרצה בדף הבית.
- **`cat-*`** (5) — כרטיסי הקרוסלה.
- **`chef-1..14`** — קרוסלת השף. **`tabon-1..12`** — קרוסלת הטאבון (12, לא 11).
  מוצגת אחת בכל פעם לפי הלשונית. **בלי נקודות** — מעל 6 תמונות הן עמוסות.
- `Photo.tsx` נופל למציין מקום מקווקו עם אייקון תמונה (בלי טקסט — בקנבס אין שם מילה).
- **`Photo` מקבלת `title`** · השם מוצג מעל התמונה המוגדלת. המקור הוא
  `data/photoTitles.ts`, שריק ומחכה לרשימה של שקד.
- ⚠ **שלוש תמונות חדשות ב-`assets/originals/`** שהיא הניחה ב-14 בספטמבר:
  `hagasha-ishit.png`, `kinuhim-table.png`, `drink-no-limit.png`.
  לא עברו `sync-photos.sh` ולא מופו. לשאול איזה שם שייך לאיזה סעיף במסך השף.

---

## החלטות שהתקבלו — לא לפתוח מחדש

1. **מע״מ** — אין. הוסר לגמרי מ-`Admin`. עוסק פטור.
2. **רווח** — הכנסות פחות הוצאות. `Admin` ו-`AdminMoney` מסכימים.
   `extract-admin-home` נכשל אם החשבון לא נסגר.
3. **עלויות `AdminMenu`** — מחושבות מעץ המתכונים של `AdminCosts`.
4. **תמונות `home`** — ברצועה הרצה. `cat-*` בכרטיסים.
5. **קרוסלות השף** — אחת בכל פעם לפי הקטגוריה, בלי נקודות.
6. **״שולם בפועל״ ב-`AdminShopping`** — אין צורך בשדה עריכה.
7. **שורת הסיכומים ב-`AdminBoard`** — להשאיר מיושר.
8. **יחיד ורבים** — שמונה ניסוחים אושרו (״הזמנה אחת״, ״פריט אחד״, ״קנייה אחת״...).
   הלוגיקה ב-`src/text/counts.ts`, ובקנבס מוטבעת בכל מסך.
9. **מגשי פירות לא עוברים באתר** — הם נעשים אצל מיכל גורן. המסך **בלי התחברות
   ובלי זרימת מסירה**; הכפתור פותח וואטסאפ עם ההזמנה. `screens/fruit/whatsappOrder.ts`,
   ⚠ המסמך טען פעם שזה ״מכוסה בשלוש בדיקות״ — **אין שם בדיקות.** נבדק ב-14 בספטמבר.
10. **כמה מארזי שניצל בהזמנה אחת** — `boxes` היא רשימה, כמו החלות הבודדות.
    ⚠ **השינוי חצה לשרת** — `orders/details.ts`, `catalog/quote.ts`, `admin/sold.ts`.
11. **מארז פרימיום ירד מהאתר** — מסונן ב-`useBoxesOrder.ts` (`HIDDEN`), הנתונים נשארו.
    ⚠ הסינון מזיז אינדקסים — `box` חייב לאנדקס את הרשימה המוצגת.
12. **כפתור ניהול ירד מדף הבית** — הגישה לניהול תהיה רק דרך ההתחברות של שקד.
    בינתיים רק `?screen=admin` בדפדפן.
13. **בדף הבית לפני התחברות כפתור אחד** — ״להתחברות והזמנה״, מוביל לטאב הכניסה.
14. **תמונות הספיישל ריבועיות** — שקד בחרה ריבוע. קבצי המקור 480×480, ומסגרת
    עומדת הייתה חותכת מהצדדים.
15. **שלושה נוסחים של Claude אושרו** — הודעת הוואטסאפ למיכל, ״להזמנה בוואטסאפ״,
    ״בחירת צורה״. **מעכשיו הם טקסטים של שקד** וחל עליהם כלל הברזל.
16. **ההתחברות היא שכבה ולא מסך** — אחרת כל זרימת הזמנה מאבדת את המצב.
17. **הבועות בשורת הקטגוריות בלי נקודת אור** — שלוש השכבות כבויות. שונה מהקנבס.

### כמה שינויים אינם מהקנבס — לדעת לפני שנוגעים

הזוהר של כפתור ההתחברות ושל הכרטיס הראשי · `TIGHTEN_TOP`/`TIGHTEN_BOTTOM` ·
`RAIL_GAP` · `HOME_PAD_NAV` · `BAR_BOTTOM_WITH_NAV` · `EyeToggle` ·
`spark={false}` · שורת המכירה הלחיצה · כותרת ״ההזמנה שלי״ · סדר המארזים
בשניצל · שורות הפעילות בכותרת הפירות · ה-timeout ב-`client.ts`.
**כולם מסומנים `⚠ לא מהקנבס` בקוד עצמו.**

---

## אימות בדפדפן — איך באמת לוחצים

`computer left_click` מחזיר `timeout` בסביבה הזו. **אבל אפשר לאמת הכל דרך
`javascript_tool`** — זה נפתר ב-11 בספטמבר ופתח את כל המסכים שהיו ״לא מאומתים״.

**לחיצה על `Pressable`** · חייבים את הרצף המלא. `click` לבדו לא נקלט:

```js
window.__press = (el) => {
  const r = el.getBoundingClientRect();
  const x = Math.round(r.left + r.width / 2), y = Math.round(r.top + r.height / 2);
  const b = { bubbles: true, cancelable: true, composed: true, clientX: x, clientY: y,
              view: window, button: 0, isPrimary: true, pointerId: 1, pointerType: 'mouse' };
  el.dispatchEvent(new PointerEvent('pointerdown', { ...b, buttons: 1 }));
  el.dispatchEvent(new MouseEvent('mousedown', { ...b, buttons: 1 }));
  el.dispatchEvent(new PointerEvent('pointerup', { ...b, buttons: 0 }));
  el.dispatchEvent(new MouseEvent('mouseup', { ...b, buttons: 0 }));
  el.dispatchEvent(new MouseEvent('click', { ...b, detail: 1 }));
};
window.__byText = (t) => [...document.querySelectorAll('*')]
  .find((e) => e.childNodes.length === 1 && e.childNodes[0].nodeType === 3 && e.textContent === t);
```

היעד הוא בדרך כלל `__byText('טקסט').parentElement`, ולכרטיס שלם
`.closest('div[class*="css-view"]').parentElement`.

**גרירה על `PanResponder`** · `TouchEvent` עם `Touch` אמיתי, כמה `touchmove`
ואז `touchend`. עובד — כך אומתה קרוסלת דף הבית.

**טעינה מחדש** · `navigate` לבדו לא תמיד מרענן. `window.location.replace(url + '&t=' + Date.now())`.

### ארבעה לקחים מ-14 בספטמבר

**1 · `computer left_click` עם `ref` עובד.** ההערה הישנה ש״הוא מחזיר timeout״
כבר לא נכונה. `find` ואז `left_click` עם ה-`ref` הוא הדרך המהירה,
ו-`browser_batch` מריץ רצף שלם בסיבוב אחד.

**2 · ⚠ `Modal` של React Native Web לא נכנס ל-`document.body.innerText`.**
זה כמעט גרם לדיווח על באג שלא קיים: לחצתי על כרטיס שניצל, החלונית נפתחה,
ו-`innerText` לא הראה כלום. **חלוניות מאמתים בצילום מסך או ב-`elementFromPoint`,
לא בטקסט.** (חסם ההתחברות דווקא כן מופיע — אל תסתמך על אף אחד מהשניים.)

**3 · לבדוק חסימות עם `elementFromPoint`, לא בעין.** ככה נתפס שחלון הקרוסלה
בולע את שורת המכירה וששורת הסה״כ חבויה מתחת לנאב. הדפוס:
```js
const r = el.getBoundingClientRect();
const hit = document.elementFromPoint(r.left + r.width/2, r.top + r.height/2);
el.contains(hit) || el === hit   // false = משהו חוסם
```
לשורה שלמה — לדגום עשר נקודות לרוחבה.

**4 · ⚠ `location.href` ו-HMR מאפסים את ההתחברות.** באמצע בדיקה ארוכה הדף
חוזר לפעמים ל-`guest` או למסך אחר. **לוודא באיזה מסך אתה נמצא לפני כל מדידה**
(`document.body.innerText.split('\n')[0]`), אחרת מודדים את המסך הלא נכון.

⚠ **חלון קופץ עוצר הכל.** כפתור הוואטסאפ במסך הפירות פותח `api.whatsapp.com`
בחלון חדש, ואז `javascript_tool` מסרב לרוץ בכל הלשוניות. לקלוד אין הרשאה
לסגור אותו — **צריך לבקש משקד.** להיזהר מללחוץ עליו באימות.

⚠ **העקיפה שעובדת: Playwright MCP.** זה דפדפן נפרד לגמרי שהחלון הקופץ לא
משפיע עליו. כל האימות בסבב ב׳ רץ דרכו. לפני כל תרחיש **לנקות `localStorage`**
(מפתח הטוקן `bite-and-tell.token`), אחרת מודדים מסך של משתמש קודם.

**שמות המסכים בקישור העומק** הם המפתחות ב-`SCREENS`: `guest` · `main` ·
`login` · `cous` · `schn` · `box` · `fruit` · `chef` · `orders` · `profile` ·
`admin`… שם לא מוכר **נופל בשקט ל-`guest`** ולא זורק שגיאה.

**עדיין לא נבדק ויזואלית:** שורות ״איסוף עצמי״ ו״משלוח״ בזרימת המסירה.
(כניסת המנהלת **כן** אומתה בסבב ב׳.)
כשמשהו לא נבדק — **להגיד לשקד במפורש.**

---

## סבב 14 בספטמבר · חלק ב׳ · המנה הגדולה של שקד

שקד שלחה **מנה אחת של 41 משימות** בצד הלקוחה, ואמרה שאחרי שנסגור את הצד
הזה נעבור לעיצוב הצד הניהולי. הסעיף הזה הוא **הפנקס** של המנה — מה נסגר,
מה נשאר, ומה חסום. **לא למחוק שורות כשמסיימים** — לסמן ✅ ולהשאיר.

שבעה קומיטים: `555c9aa` · `8512c6d` · `456264a` · `c06dd06` · `24807ef` ·
`944d051` · `fdb33d9`. כולם נדחפו ל-`main`, וכולם אומתו במדידה בדפדפן.

### ✅ מה נסגר (15 מתוך 41)

| מה | איפה | מה נמדד |
|---|---|---|
| כניסת המנהלת למסך הניהול | `navigation/store.tsx` | שלושה מסלולים + בדיקת נסיגה של לקוחה |
| 36 שמות התמונות | `data/photoTitles.ts` | מופיעים על הרצועות והסטריפים |
| כיתוב על כל תמונה בקרוסלה | `PhotoCaption.tsx` · חדש | 10.5 ברצועה, 15 בסטריפ |
| לייטבוקס מיידי בלי טשטוש איטי | `Lightbox.tsx` | `animationType` מ-`fade` ל-`none` |
| פינות עגולות ויחס תמונה אמיתי בלייטבוקס | `Lightbox.tsx` | `SHOT_RADIUS 18` |
| כפתור המשך אחד משותף | `ContinueButton.tsx` · חדש | החליף **7** מופעים כפולים |
| כפתור המשך · זכוכית + ידית לבנה, רוחב מינימלי | `ContinueButton.tsx` | 97×38 אחרי ה״מעט רוחב״ |
| כפתור התנתקות גלובלי | `LogoutButton.tsx` · חדש | מותקן פעם אחת ב-`App.tsx` |
| חץ חזרה אחד משותף וקטן יותר | `BackButton.tsx` · חדש | החליף **4** מופעים כפולים |
| החץ ההפוך בספיישל הפנימי | `BackButton.tsx` | שני החצים `M9 18l6-6-6-6` |
| רווח מתחת למחיר בעמוד המארז | `BoxesScreen.tsx` | `DETAIL_PAD` 18→26 |
| סלטים בגרם ובקפיצות 250 | קנבס + `data/boxes.ts` | 4 שורות בלבד השתנו |
| נוסח ״שנה טובה״ | קנבס + `data/boxes.ts` | |
| יחידת המידה מתחת למספר | `SectionRenderer.tsx` | מספר ב-312, יחידה ב-332 |
| מרווח עליון לכותרות + כרטיסי סוג האירוע | `SectionRenderer.tsx` · `eventPhotos.ts` | 12px, וכרטיס 60→**132** |

### ⛔ ארבעה חסמים — אסור להתחיל בלעדיהם

1. **מקור נתוני הכתובות.** Google Places עולה כסף ודורש חיוב; `data.gov.il`
   חינמי ורשמי. **ההמלצה: data.gov.il.** שקד עוד לא ענתה.
2. **מחירי המשלוח לפי אזור** — יבנה · אשדוד · ראשון לציון · גדרה.
   **אין לנו אותם. אסור להמציא.**
3. **איך התזכורת מגיעה ללקוחה** כשהמכירה נפתחת. ההמלצה: שמירה בשרת +
   רשימה בממשק הניהול עכשיו, Expo Push בהמשך.
4. **חלון הוואטסאפ הקופץ עדיין פתוח** וחוסם את הדפדפן המובנה לגמרי.
   **רק שקד יכולה לסגור אותו.** העקיפה: Playwright MCP — דפדפן נפרד שלא מושפע.

### 📋 מה שנשאר מהמנה — בניסוח של שקד

**ספיישל (קוסקוס · שניצל · מארזים)**
- ״סגור להזמנות״: **בלי** אזהרה בתשלום. במקום — פופאפ עם אייקון פעמון על
  ״המשך״, ששואל אם רוצים תזכורת כשהמכירה נפתחת. ⚠ **הכלל הזה חל רק על
  ימי המכירה של הקוסקוס והשניצל** — כל שאר ההזמנות תמיד נכנסות.
- שניצל: הסה״כ חייב לספור **מארזים ובודדים יחד** בהזמנה אחת;
  לחסום משלוח מתחת ל-5 מנות או מתחת למארז אחד.

**מגשי פירות**
- ״לפרטים נוספים״ מחייג למספר הרשום.
- כפתור הוואטסאפ פותח **קודם** פופאפ פרטים (שם · תאריך מלוח שנה עם שבת סגורה ·
  שעה · איסוף/משלוח עם עלות המשלוח), ורק אז שולח את ההודעה.

**פינת השף**
- להקטין מרווח כותרת↔תיאור; ״בוחרים את המסלול שלכם״ מקבל מרווח עליון, גדול ומודגש.
- חצי הקרוסלה הופכים לכפתורים מרחפים שקופים למחצה שעובדים **תמיד בשני הכיוונים**.
- רוחב העמוד הראשי = רוחב העמוד שאחרי הבחירה; אחרי בחירת מסלול **אין** קרוסלה ואין תיאור.
- מרווח בין פס ההתקדמות לשאר המסך; ״סועדים״ כתוב מתחת למספר.
- **בכל העמודים הפנימיים** מרווח עליון בין כל כותרת פנימית למה שלפניה,
  וגודל כרטיסים אחיד בכל קטגוריה. ״אני רוצה את זה מדויק לאיך שזה נראה שם.״
- טופס יצירת הקשר: תאריך מלוח שנה עם ימים לא פעילים באפור, שישי ערב חסום,
  שבת בוקר וצהריים חסומים; כתובת בהשלמה אוטומטית תוך כדי הקלדה.
- אחרי ״לבקשת הצעה״ **אין** ״איך תרצי לקבל״ — במקומו עמוד אישור עם סיכום
  ההזמנה המלא כולל שדרוגים ותוספות ומחירים, תאריך ומקום האירוע, והערה
  שייצרו קשר לאישור ומקדמה. **״שים לב לנראות בקנבס של כל העמוד הזה!!״**

**ארוחת שף פרטית**
- כל הסגנונות תמיד גלויים, אבל נעולים ואפורים כשנבחר ציר אחר.
- תיאור מתחת לכל כמות מנות עם המחיר לסועד.
- `arichat-shulhan` מעל ״עיצוב שולחן יוקרתי״ · `drink-no-limit.png` מעל ״שתייה ללא הגבלה״.

**עמדת טאבון**
- ״X ש״ח לסועד״ מתחת לכמות הסועדים, ומתחתיו ״המחיר משתנה בהתאם לכמות
  הסועדים״ — באותו סגנון של ארוחת השף.
- רוחב אחיד לכל כרטיסי הסלטים/פסטה/קינוחים.
- בבחירה מקסימלית **לא לנעול** את השאר — פופאפ: ״נבחרה הכמות המקסימלית,
  כל בחירה נוספת היא שדרוג ובעלות נוספת.״
- למחוק את השורה הכפולה (״כל בחירת סלט נוסף בתוספת 20 ש״ח לסועד״) ולהשאיר
  רק את הראשונה — **בכל קטגוריה**.
- אחרי בחירת רוטב לפסטה חייב להיפתח חלון סוג הפסטה/שדרוג — ״מדויק לקנבס״.
- שדרוגים: להוסיף ״כרובית מטוגנת״, ״מגש גבינות״, ״מגש ירקות״ למנות הפתיחה;
  ״פילה חציל״ → ״חציל בפנקו מטוגן״ עם ״קרפצ׳יו חציל״ לצידו.
- ארבעת השדרוגים מתחת למנות הפתיחה — **מדויק לקנבס כולל תמונות ומחירים**:
  ״מנת דג בטאבון״ `tabon-4` · ״שולחן קינוחים מעוצב״ `kinuhim-table.png` ·
  ״ממשותף לאישי״ `hagasha-ishit.png` · ״עיצוב שולחן יוקרתי״ `arichat-shulhan`.

**רוחבי האפליקציה**
- אמצעי תשלום: אייקון מעל התיאור של כל אפשרות, **2 בשורה**, מסגרת עוטפת בהירה יותר.
- שדה כתובת: השלמה אוטומטית על כתובות בישראל; **מחוץ** לגבול החלוקה →
  אדום + ״אין משלוחים לאיזור הזה״ + ״המשך״ נעול; **בתוך** → ירוק + עלות
  המשלוח מתחת. ⛔ חסום על 1 ו-2.
- ״ההזמנות שלי״ · ״להזמין שוב״ בודק קודם אם יום המכירה פתוח: אם כן — פותח
  את המכירה עם הפריטים מסומנים מראש; אם לא — פופאפ הפעמון.
- פרופיל: העלאת תמונה לא עובדת; להוסיף שדה מין כדי לדעת אם זו לקוחה או לקוח.
  ⚠ **דורש עמודה חדשה ב-`User` — צד שרת, כלומר קורסור.**
- אנימציות ברחבי האפליקציה בהשראת `dominos.co.il`. הצעתי להביא קודם רשימה
  קצרה ומדודה לאישור, ולא לפזר אנימציות מיוזמתי.
- שלוש התמונות שעוד לא סונכרנו ב-`design/app/assets/originals/`:
  `drink-no-limit.png` · `hagasha-ishit.png` · `kinuhim-table.png`.

**שאלה פתוחה שהועלתה לשקד ולא נענתה:** ״חגיגה בשולחן • סלטים״ עדיין כתוב
״צנצנות כ־250 מ״ל״. לא נגעתי — הצנצנות הן נפח, והבקשה שלה נקבה רק
ב״קחו כמה שבא לכם״.

### חמישה לקחים חדשים מהסבב הזה

**1 · ⚠ `Image.resolveAssetSource` לא קיים ב-react-native-web.** קריאה אליו
הפילה את האפליקציה למסך לבן. ב-web ה-`require` של תמונה מחזיר אובייקט עם
`width`/`height` — לקרוא משם, ורק אם אין לנסות את ה-API הילידי.

**2 · ⚠ `alignSelf: 'center'` בלי `width: '100%'` מכווץ מדידות.** המסגרת
מתכווצת לרוחב התוכן, `onLayout` מחזיר רוחב זעיר, וכל רוחב שנגזר ממנו קורס.
ככה כרטיסי ״סוג חלה״ יצאו 60 במקום 132. **כשמשתמשים ב-`onLayout` — לוודא
שלהורה יש רוחב מפורש.**

**3 · ⚠ אל תכתוב emitter חדש לקובץ שנכתב ביד.** ה-emitter שכתבתי ל-`boxes.ts`
מחק שמות תמונות, `square: true` ו-`as const` — שדות שלא מגיעים מהקנבס בכלל.
נתפס רק כי שמרתי עותק לפני. **לקובץ שנכתב ביד עושים החלפות ממוקדות,
כשהערכים מגיעים מפלט ה-JSON של המחלץ ולא מהקלדה.**

**4 · `PhotoCaption` חייב `pointerEvents="none"`.** בלעדיו הכיתוב בולע את
הלחיצה על הרצועה והלייטבוקס לא נפתח.

**5 · כפתור מרחף גלובלי מתנגש במסכים אחרים.** כפתור ההתנתקות בלע את ״לחנות״
במסך הניהול. נתפס עם `elementFromPoint`, לא בעין. הפתרון:
`if (screen.startsWith('admin')) return null;`.

### שלושה רכיבים משותפים חדשים — להשתמש בהם, לא להעתיק

`ContinueButton` · `BackButton` · `PhotoCaption`.
יחד הם החליפו 13 מופעים מועתקים. **כפתור המשך או חץ חזרה חדש = לקרוא לרכיב.**
⚠ שלושתם **אינם מהקנבס** במראה שלהם — שקד בחרה אותם מתוך הצעות.

### קבצים חדשים שנכתבו ביד — לא לגעת בלי שקד

| קובץ | מה יש בו |
|---|---|
| `mobile/src/data/photoTitles.ts` | 36 שמות תמונות **משקד**. אין מחלץ. **אין להמציא שמות.** |
| `mobile/src/screens/boxes/eventPhotos.ts` | 4 תמונות אירוע בחלה **משקד**. `UNMAPPED_EVENTS` תופס אירוע חדש בקנבס בלי תמונה. |

---

## איך לעבוד מול שקד

- עברית, ישיר, בלי להתנצל.
- **לאמת, לא להניח.** למדוד בדפדפן ולהראות מספרים.
- כשמשהו נשבר — להגיד מה, למה, ואיך תוקן.
- **כשמשהו לא נבדק — להגיד את זה במפורש.** זה קרה בסשן הזה: דיווחתי על
  ״באג בלקוחות״ שהתבסס על קטע טקסט קטוע, ולא היה שום באג.
- להציע במקום להוסיף מיוזמתי.
- **חלוקה מול קורסור:** הוא על השרת, Claude על האפליקציה, הקנבס והצינורות.
  קורסור לא נוגע ב-`mobile/src/data/` ולא ב-`design/app/`.
