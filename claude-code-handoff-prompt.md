# שקד לילוז · שאלון הזמנות — מסמך המשכיות

> עודכן: 6 באוגוסט 2026. הקובץ מתאר את המצב **הנוכחי** של הפרויקט.
> אם קראת אותו — יש לך את כל מה שצריך כדי להמשיך בלי שאלות פתיחה.

---

## הפרויקט

קובץ HTML **יחיד** — `chef-questionnaire.html` (~3,270 שורות, ~265KB).
שאלון הזמנות אינטראקטיבי לעסק קייטרינג/שף פרטי.
Vanilla HTML/CSS/JS. בלי build, בלי frameworks, בלי dependencies חיצוניים
מלבד גופנים מ-Google Fonts שנטענים ב-`<link>`.
מבנה: סקריפט קטן ב-`<head>` (מצב תצוגה), `<style>` אחד גדול, ו-`<script>` אחד
גדול בתחתית. **RTL מלא.**

---

## מבנה הניווט · ארבע רמות

```
מסך ראשי (3 מרכזים)  →  מסך משנה (2–3 קטגוריות)  →  הזרימה  →  סיכום
        ↓
   עמוד האודות
```

המשתנה `view` מחזיק את המסך הנוכחי: `'hub'` · `'sub'` · `'about'` · `'flow'`.
`hubKey` מחזיק איזה מרכז פתוח. `idx` הוא השלב בתוך הזרימה (`-1` = לא בזרימה).

**חשוב:** המשתנה נקרא `view` ולא `screen` — `screen` מתנגש עם `window.screen`.

### שלושת המרכזים

| מפתח | כותרת | קטגוריות בפנים |
|---|---|---|
| `home` | רגעים של בית 🏡 | שלישי של קוסקוס · מסודרים לשישי |
| `host` | מארחים בסטייל 🥂 | מארזי חג 🎁 · מגשי אירוח 🥗 · מגשי פירות 🍓 |
| `event` | חוויה בלתי נשכחת 👨🏻‍🍳 | ארוחת שף פרטית · עמדת טאבון |

הנתונים ב-`HUBS` ו-`SUB_ITEMS`. **מסך המשנה הוא אותו רכיב קרוסלה של המסך
הראשי** — `renderCover()` מזין אותו ברשימת פריטים אחרת. אין שכפול.

### שבע הזרימות

| flow | שלבים | תיאור |
|---|---|---|
| `chef` | c1–c6 + last | ארוחת שף |
| `taboon` | t1–t7 + last | עמדת טאבון |
| `holiday` | h1–h3 | מארזי חג |
| `friday` | h1–h3 | מסודרים לשישי |
| `trays` | h1–h3 | מגשי אירוח |
| `fruit` | h1–h3 | מגשי פירות |
| `couscous` | **h2–h3** | שלישי של קוסקוס — אין עמוד בחירת מארז |

חמש הזרימות עם h1 חולקות שלבים; ההפרדה דרך שדה `cat` ב-`BOXES`.
`last` משותף ל-chef ול-taboon.

**להוספת קטגוריית מארזים חדשה** — שורה ב-`FLOWS`, שורה ב-`BOX_FLOWS`,
רשומה ב-`SUB_ITEMS` תחת המרכז הנכון, ורשומות ב-`ORDER_LABEL` / `CAT_COPY` / `BOXES`.
**אין צורך לגעת בשום תנאי לוגי אחר** — `isBoxFlow()` מרכז את כולם.

---

## שפת העיצוב · Liquid Glass · חצות ונחושת

כל הצבעים והחומרים הם משתני CSS. שינוי פלטה = עריכת המשתנים בלבד.

- **שני מצבי תצוגה.** `:root` הוא הכהה; `:root[data-theme="light"]` הוא הבהיר.
  **בבהיר סולם הנחושת מתהפך:** `--brass-lit` הוא הגוון הכהה, כי הוא זה שצריך
  להיקרא על רקע בהיר. `--oyster` בבהיר הוא `#553A26` — חום כהה מכוון,
  לא כמעט־שחור. יחס ניגודיות 9.86:1.
- **מסך פתיחה.** לוגו ← פתיחת הסיפור ← ״מה מרימים היום?״ ← קרוסלת `scroll-snap`
  עם 3 כרטיסים ← מגש 3 מכסים קטנים שמשמש גם כתצוגה וגם כניווט.
  `IntersectionObserver` קובע מי במרכז.
- **פתיחת הסיפור.** `.teaser` — שורת פתיחה גדולה ב-Suez One עם ברק נחושת
  שעובר עליה פעם אחת, משפט תומך, קו נחושת דק, וכפתור ״לסיפור המלא״.
  בלי מסגרת — טקסט חופשי.
- **כפתור ״לסיפור המלא״.** בלי מסגרת. קו נחושת נמשך מימין לשמאל, ומכסה קטן
  מתרומם ומעלה אדים. יש גם מצב `:active` כי **בנייד אין ריחוף**.
- **שלבים פנימיים.** כל שלב הוא לוח זכוכית (`.step.sheet`) עם מכסה שמתרומם
  בכניסה וקו צלחת נחושתי מתחת לכותרת. מוזרקים ב-JS ב-`dressSteps()`.
- **אריחי אבן + אור נוזלי.** אור שעוקב אחרי האצבע (`--mx`/`--my`), גל בחירה
  (`--rx`/`--ry`), וקו נחושת שמקיף את הנבחר. הכל דרך `::before`/`::after` —
  **כך גם רכיבים שנוצרים ב-JS מקבלים את ההתנהגות אוטומטית**.
- **סרגל עליון:** לוגו · מכסי התקדמות במרכז · חץ חזרה. מוסתר **רק במסך הראשי**
  (`view!=='hub'` מפעיל `has-chrome`) — במסך המשנה הוא נשאר בשביל חץ החזרה.
  **מתג התצוגה יושב מחוץ לסרגל** (`.themex`, `position:fixed`).
- **מזח תחתון:** ״המשך״ + סה״כ חי. נעלם בשלבים שמתקדמים מעצמם (h1, c2, c3).
- **עמוד המארזים (h1):** שורות תפריט — תמונה משמאל, טקסט מימין, מחיר בתחתית.
  כותרת דביקה. תת־קטגוריות דרך שדה `group` (כרגע אין משתמשים).
- **תאריך ושעה:** שדות מתקפלים עם `__/__/____` ו-`__:__`. בלחיצה נפתחת
  חלונית מקובעת מעל המזח עם רקע מעומעם.

### סמן העכבר

סכין ומזלג מוצלבים (`#curx`) שנפתחים לצדדים מעל כל דבר לחיץ.
**נדלק רק כש-`initCursor()` מוודא `(hover:hover) and (pointer:fine)`** ומוסיף
`body.has-cursor`. הסתרת הסמן הרגיל תלויה במחלקה הזו — כך שבנייד או בלי JS
הסמן הרגיל נשאר. מעל `input`/`textarea`/`select` הסמן המותאם נעלם וקו
ההקלדה חוזר.

### מעברים בין המסכים

| מעבר | אפקט |
|---|---|
| מהמסך הראשי → מסך משנה | `x-lid` — מכסה יורד, סוגר, מתרומם |
| כל שאר המעברים | `x-out` / `x-in` — הקלפים עפים ומתחלפים |

מנוהל ב-`coverSwap(kind, fn)`. כשיוצאים ממסך בלי כרטיסים (שלב בזרימה או
האודות) מדלגים על שלב היציאה, אחרת נוצרת השהיה שבה כלום לא קורה.

---

## החלטות ארכיטקטורה שחשוב לא לשבור

1. **הטקסטים חיים במקום אחד.** רשימת הרטבים ב-t4 יושבת ב-`<template id="sauce_tpl">`
   ומשוכפלת ב-JS לשתי הפסטות. אל תשכפל אותה ב-HTML.

2. **`<br>` חייב לעבוד בכל טקסט.** כל הזרקת תוכן משתמשת ב-`innerHTML` ולא
   ב-`textContent`. בהודעת הוואטסאפ `plain()` הופך `<br>` לירידת שורה ומנקה תגיות.

3. **`backdrop-filter` לא מטשטש מתחת ל-`position:sticky`** (בסרגל העליון כן,
   כי הוא `fixed`). לכן הכותרת הדביקה ב-h1 והחלוניות המוקפצות חייבות
   **רקע אטום**. אל תנסה שוב לתת להן מראה זכוכית.

4. **`.step.sheet` יוצר הקשר ערמה** (בגלל `backdrop-filter`) וכולא כל `z-index`
   פנימי. לכן חלוניות התאריך/שעה מועברות ב-JS אל `.wrap` בזמן הבנייה.

5. **שמירת מקום למזח נקבעת ב-JS** (`syncDockHeight`) ולא ב-CSS.

6. **אסור לגעת ב-`transform` של `.slab`.** זהו אזור ה-`scroll-snap`, ושינוי
   גיאומטריה שלו — אפילו באמצע אנימציה — מקפיץ את הקרוסלה לכרטיס השכן.
   כל אנימציית מעבר רצה על `.slab-in` הפנימי. **הקדר האחרון של כל אנימציה
   חייב להתאים בדיוק ל-CSS הרגיל** (`scale(.91)` לצדדיים, `none` לפעיל),
   אחרת עם `animation-fill-mode:both` הכרטיסים נתקעים בגודל הלא נכון.

7. **בית הקרוסלה הוא `scrollLeft:0`** — הריפוד הצדדי של `.crail` ממרכז את
   הכרטיס הראשון בדיוק שם. `coverHome()` מכבה `scroll-snap` לרגע, מאפס, ומחזיר.
   `settleCover()` מתקן חמש פעמים בשנייה הראשונה (גופנים שנטענים מזיזים את
   ה-snap) **ומפסיק ברגע שהמשתמשת נוגעת** — `coverTouched`.

8. **ב-RTL הסימן של `scrollLeft` שונה בין דפדפנים.** כל מרכוז קרוסלה מחושב
   כהפרש מרכזים דרך `getBoundingClientRect` + `scrollBy`.

9. **אל תוסיף טריקים של `100vw` / negative margin.** רוחב רחב נשלט דרך
   `body.is-wide` בלבד.

10. **כפתורים לא יורשים צבע.** יש `button{color:inherit}` גלובלי — אל תסיר.

11. **`wrapDigits` עוטף מספרים ב-`.num` (Karantina) ו-₪ ב-`.cur` (Heebo).**
    יש `MutationObserver` שמריץ את זה מחדש — היזהר מלולאות. הוא צופה ב-`childList`
    ו-`characterData` בלבד, ולכן שינויי `style`/`class` לא מפעילים אותו.

12. **גבהים אחידים בעמוד המארזים** נשענים על `grid-auto-rows:1fr`. הרשת יושבת
    על `.box-grid` של כל קבוצה בנפרד ולא על `#boxes`.

13. **מצב התצוגה נקבע בסקריפט שב-`<head>`**, לפני הציור הראשון, אחרת יש הבזק
    של המצב הלא נכון. סדר: בחירה ב-`localStorage` ← `prefers-color-scheme` ← כהה.

14. **`paintSaladQty` מזהה את רשת הסלטים דרך `[data-cap]`** — רשת מנות הקוסקוס
    חולקת את אותו עיצוב (`.sq-grid`) ובלי המבדיל הזה הן מתנגשות.

15. **`background-clip:text` עטוף ב-`@supports`.** בלי תמיכה שורת הפתיחה
    הייתה נעלמת לגמרי במקום להיראות רגילה.

---

## איפה עורכים תוכן

יש מדריך מלא ללקוחה: **`עריכת-טקסטים.html`** (פותחים בדפדפן).
**המדריך מתאר את המבנה הישן ולא עודכן — צריך רענון.**

בקצרה — הכל בתחתית ה-`<script>`:

| קבוע | מה |
|---|---|
| `ABOUT_TITLE` / `ABOUT_TEASER_LEAD` / `ABOUT_TEASER_SUB` / `ABOUT_BODY` | עמוד האודות והפתיח במסך הראשי |
| `HUBS` | 3 הכרטיסים הראשיים — `eb` (משפט קטן), `name`, `desc` |
| `SUB_ITEMS` | הקטגוריות בכל מרכז |
| `CAT_COPY` | כותרות עמוד המארזים (לכל קטגוריה בנפרד) |
| `BOXES` | כל המארזים — `name`, `desc`, `price`, `sub`, `group` |
| `MEALS` / `MEAL_ADDONS` / `MEALS_NOTE` | מנות הקוסקוס והתוספות |
| `COUS_MIN_DELIVERY` | מינימום מנות למשלוח בקוסקוס |
| `EREV_CHAG` | ערבי חג — משפיע על שעות האיסוף במגשים |
| `BOX_GROUP_NOTE` | הערה מתחת לכותרת תת־קטגוריה (כרגע לא בשימוש) |
| `ORDER_LABEL` | שם הקטגוריה בסיכום ובוואטסאפ |
| `SALADS` | `[שם, תיאור]` |
| `CHALLAHS` / `WINES` / `CAKES` / `PROTEINS` | רשימות בחירה |
| `PASTA_SHAPES` | 7 צורות פסטה + ה-SVG שלהן |
| `CHEF_PHONE` / `FRUIT_PHONE` / `CHEF_PRICES` / `TABOON_TIERS` | טלפונים ומחירים |
| `PHONE_MSG` | הודעת שגיאת אימות הטלפון |

**כלל:** גרש בתוך טקסט חייב להיות `׳` ולא `'`, וגרשיים `״` ולא `"` —
אחרת מחרוזת ה-JS נשברת.

**האימוג׳י בשמות הקטגוריות מסונן אוטומטית** במגש המכסים הקטן (`noEmoji()`).
אין צורך לתחזק שני שמות.

---

## שעות איסוף · לפי קטגוריה

הכל ב-`getWindows(dateStr)`. `genSlots(windows, step)` מייצר את השעות.

| קטגוריה | שעות |
|---|---|
| מסודרים לשישי | ה׳ 17:00–20:00, ו׳ 9:00–15:00 · שניצל: ו׳ 11:00–15:00 |
| מארזי חג | ו׳ 9:00–15:00 · `HOLIDAY_SPECIAL` דורס · מארז ילדים 7:00–11:00 |
| מגשי אירוח + מגשי פירות | א׳–ה׳ 7:00–20:00 · ו׳ וערבי חג 7:00–14:00 · שבת וחגים סגור |
| שלישי של קוסקוס | ימי שלישי בלבד, לא בחגים · איסוף 11:30–14:30 · משלוח 12:00–14:00 |

**קפיצות בחירת השעה:** מגשי אירוח ומגשי פירות — חצי שעה (`TRAY_SLOT_MIN`).
כל השאר — 10 דקות (`SLOT_MIN`). דרך `slotStep()`.

**בקוסקוס חלון השעות תלוי ב-`H.ship`** — ולכן `renderCalendar()` מורץ מחדש
כשמחליפים בין איסוף למשלוח.

---

## מה נשאר פתוח

1. **תוכן ל״מגשי אירוח״ — הפריט היחיד שחוסם פרסום.** יש בקובץ **10 סימוני ⚠**:
   - שני מגשי האירוח (`trayA`, `trayB`) — שם, תיאור, `sub`, **ומחיר (כרגע 0)**
   - התיאור בכרטיס מסך המשנה (`SUB_ITEMS.host`, flow `trays`)
   - תת־הכותרת של העמוד (`CAT_COPY.trays.sub`)
   - **רשימת `EREV_CHAG`** — נגזרה מתאריכי 2026 שכבר היו בקובץ, לא אושרה.
   הלקוחה הודיעה שהיא עובדת על התוכן ותעדכן.

2. **התיאורים של ארבעת מגשי הפירות נכתבו על ידי Claude**, לא על ידה.
   היא נתנה שמות ומחירים בלבד (300 / 300 / 400 / 450 ₪).
   *(הטלפון של מיכל גורן — 052-2958511 — אושר על ידה ואינו פתוח יותר.)*

3. **שליחה אוטומטית לוואטסאפ.** ״סיום ושליחה״ **לא קופץ לוואטסאפ** — מציג רק
   את מסך האישור, לבקשת הלקוחה, עד שיהיה שרת. במסך האישור נשאר כפתור
   ״שליחה בוואטסאפ לשקד״ ידני. המקום שבו השליחה תיכנס מסומן בהערה ב-`next()`.

4. **תמונות המארזים.** `.box-thumb` מציג מסגרת מקווקוות עם ״תמונה בקרוב״.
   המבנה מוכן לקבל `<img>` אמיתי.

5. **תאריכי 2026 מקודדים** — `HOLIDAY_SPECIAL`, `CHEF_BLOCKED_RANGES`, `EREV_CHAG`.
   יצטרכו עדכון ידני לשנה הבאה.

6. **שלושה לוחות שנה כמעט זהים** (`renderCalendar` / `renderFCalendar` /
   `renderTCalendar`). כדאי לאחד — לא דחוף.

7. **`עריכת-טקסטים.html` לא עודכן** ומתאר את המבנה הישן.

8. **ביצועים במצב בהיר.** ערכי הטשטוש חזקים (`blur(30px) saturate(200%)`).
   לא נבדק על מכשיר ישן. אם כבד — מורידים שני מספרים ב-`--g-blur`.

9. **הספרות ב-Karantina דקורטיביות מאוד** — `0/5` נקרא כמעט כמו אותיות.
   הועלה ללקוחה, לא הוכרע.

---

## איך לעבוד כאן

- **לבדוק כל שינוי בדפדפן אמיתי, בשני מצבי התצוגה.** באגים של מרכוז,
  `z-index`, `scroll-snap` ו-`backdrop-filter` התגלו רק ברינדור אמיתי.
- **לוודא שהדף באמת נטען מחדש.** חלונית התצוגה לפעמים מתעלמת מ-`reload`
  ומחזירה את הדף הישן עם ה-state הקודם. לבדוק `performance.now()` —
  אם הוא גדול, לא היה reload והמדידה חסרת ערך. **זה הטעה כבר כמה פעמים.**
- **מדידה אחת שנפלה טוב אינה הוכחה.** באג הקרוסלה ״תוקן״ פעמיים על סמך
  מדידה בודדת שנפלה בתזמון הנכון, וחזר. לבדוק לאורך זמן ובכמה טעינות.
- **`backgroundColor` לא מספיק כדי לבדוק רקע.** אם הרקע הוא gradient הוא יחזיר
  `transparent`.
- **בדיקת ריחוף לא עובדת באמולציית נייד** — היא מתרגמת עכבר למגע.
  לעבור ל-desktop preset לפני בדיקת `:hover`.
- **קואורדינטות של `hover`/`click` הן במרחב הצילום ולא ב-CSS.** להכפיל לפי
  היחס בין גודל הצילום לגודל ה-viewport.
- **עריכות דרך רג׳קס מסוכנות בקובץ הזה.** להעדיף התאמות מחרוזת מדויקות,
  ואחרי כל עריכה: `node --check` על ה-JS + ספירת רכיבים בדפדפן.
  את ה-JS מחלצים כך:
  ```
  python3 -c "import re;s=open('chef-questionnaire.html',encoding='utf-8').read();print(re.findall(r'<script>(.*?)</script>',s,re.S)[-1])" > /tmp/app.js && node --check /tmp/app.js
  ```
- **גיבוי לפני עריכה גדולה.**
- **הלקוחה עורכת טקסטים בעצמה בקובץ.** לפני עריכה — לקרוא את המצב הנוכחי
  ולא להסתמך על טקסט שמופיע כאן. **אסור לשנות טקסטים שהיא כתבה.**
- **כשמוסיפים טקסט חדש — לומר לה במפורש שהוא נכתב על ידך**, כדי שתאשר או תחליף.

---

## קבצים בתיקייה

| קובץ | מה |
|---|---|
| `chef-questionnaire.html` | **האתר עצמו** |
| `עריכת-טקסטים.html` | מדריך עריכה ללקוחה — **מיושן, צריך רענון** |
| `chef-questionnaire.backup6-pre-hours-20260806-1437.html` | לפני שעות האיסוף ושינוי שם החג |
| `chef-questionnaire.backup5-pre-hub-20260806-1320.html` | **לפני מבנה שלוש הרמות** |
| `chef-questionnaire.backup4-pre-theme-20260803-1118.html` | לפני עבודת הערכות |
| `chef-questionnaire.backup3-20260802-2246.html` | לפני סידור הטופס הראשוני |
| `chef-questionnaire.backup.html` · `chef-questionnaire.backup2.html` | ישנים |
| `options-round3.html` | סבב אופציות — עכבר · כפתור · מעברים. **הוכרע** |
| `options-round4-cursor.html` | סבב עכבר שני. **הוכרע — נבחר ״סכו״ם מוצלב״** |
| `light-options.html` · `light-options-2.html` · `light-glass.html` · `button-options.html` | סבבי המצב הבהיר — הסתיימו, אפשר למחוק |
| `cover-options.html` · `step-options.html` · `step-options-2.html` · `tile-wow.html` · `box-options.html` | סבבי עיצוב ישנים — אפשר למחוק |
