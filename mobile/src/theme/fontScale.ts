/**
 * הגדלה גלובלית של הכתב באפליקציה.
 *
 * ⚠ **נבנה ב-17 בספטמבר 2026** · שקד: ״מרגיש לי שהכתב קטן מידי —
 * אני רוצה שתציג לי תצוגה מקדימה… גודל אחד גדול יותר, 2 גדלים,
 * … עד 5 גדלים גדולים יותר בקפיצות של 2״.
 *
 * הערך נוסף ל-`fontSize` של **כל** טקסט באפליקציה, ו-`lineHeight`
 * גדל איתו באותו יחס כדי שהשורות לא יידבקו. ההחלה היא במקום אחד
 * בלבד — `src/ui/text.tsx` — שהוא ה-`Text` שכל מסך מייבא.
 *
 * ⚠ **מה לא מושפע** · הכותרת ״Bite & Tell״ מצוירת ב-SVG
 * (`Masthead`) ולכן היא לא זזה, וזה בכוונה: היא כבר בגודל שנבחר.
 *
 * ⚠ **שקד בחרה ״2 גדלים גדול יותר״ · 17 בספטמבר 2026** · מתוך שש
 * האפשרויות שהוצגו לה בצילומים אמיתיים מהאפליקציה. 0 מחזיר בדיוק
 * למה שהיה, וזה המקום היחיד לשנות בו.
 */
export const FONT_BUMP: number = 4;

/** הגדלים שהוצגו לה · 0 הוא המצב הנוכחי */
export const FONT_BUMP_STEPS = [0, 2, 4, 6, 8, 10] as const;
