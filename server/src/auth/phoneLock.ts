import { normalizePhone } from './identity.ts';

/**
 * האם לדחות שינוי של מספר הטלפון בחשבון.
 *
 * ⚠ **נבנה ב-19 בספטמבר 2026** · בקשה של שקד: ״באזור אישי אמרנו
 * שטלפון אי אפשר לעדכן, זה צריך להיות נעול״.
 *
 * ⚠ **לא רק במסך** · השדה באזור האישי נעול, אבל נעילה בממשק היא
 * קישוט: המספר הוא **המזהה שאיתו נכנסים לחשבון**, והוא מאומת
 * בקוד חד-פעמי בהרשמה. שינוי שלו מהטופס היה מעביר חשבון למספר
 * שאיש לא אימת.
 *
 * ⚠ **אותו מספר בכתיב אחר אינו שינוי** · המסך שולח לפעמים
 * ‎050-123-4567 ולפעמים ‎+972501234567. שניהם אותו אדם, ולכן
 * ההשוואה היא אחרי נרמול ולא על המחרוזת.
 *
 * ⚠ **חשבון בלי מספר כן יכול להוסיף** · מי שנרשם בגוגל נכנס בלי
 * טלפון, וחסימה גורפת הייתה נועלת אותו בלי מספר לתמיד.
 */
export function phoneChangeBlocked(current: string | null, incoming: string): boolean {
  const now = (current ?? '').trim();
  if (!now) return false;
  const next = incoming.trim();
  if (!next) return true;
  return normalizePhone(next) !== normalizePhone(now);
}
